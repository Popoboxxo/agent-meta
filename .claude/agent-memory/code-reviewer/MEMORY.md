# Memory Index

- [orchestrator-guard false positives](orchestrator-guard-false-positives.md) — the repo's own PreToolUse hook blocks read-only Bash commands containing `merge`/`push` substrings; how to phrase around it
