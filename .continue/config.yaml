# agent-meta:managed-begin
# Managed by agent-meta v0.53.1 — 2026-05-25
# Agents : .continue/agents/  (auto-discovered by Continue)
# Rules  : .continue/rules/   (auto-discovered by Continue)
# agent-meta:managed-end
# Continue configuration
# See https://docs.continue.dev for full documentation

# Agent rules are in .continue/rules/ - managed by agent-meta
