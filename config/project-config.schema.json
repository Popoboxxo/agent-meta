{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/Popoboxxo/agent-meta/agent-meta.schema.json",
  "title": "agent-meta.config.json",
  "description": "Project configuration for agent-meta sync.py. Controls which agents are generated, which models and memory scopes are used, and how hooks and external skills are activated.",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference — enables IDE autocomplete and validation."
    },
    "_comment": {
      "type": "string",
      "description": "Optional free-text comment, ignored by sync.py."
    },
    "agent-meta-version": {
      "type": "string",
      "description": "The agent-meta version this project was last synced with (e.g. '0.16.5').",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "ai-provider": {
      "type": "string",
      "description": "AI provider (legacy single-value field). Use ai-providers for multi-provider support. Claude enables CLAUDE.md management and settings.json creation.",
      "enum": [
        "Claude",
        "Gemini",
        "Continue",
        "GitHub",
        "Opencode",
        "Mammouth"
      ]
    },
    "default-provider": {
      "type": "string",
      "description": "Provider resolve_providers() falls back to when neither ai-provider nor ai-providers is set (issue #631). Defaults to 'Claude' for backward compatibility.",
      "enum": [
        "Claude",
        "Gemini",
        "Continue",
        "Opencode",
        "Copilot",
        "Mammouth"
      ]
    },
    "ai-providers": {
      "type": "array",
      "description": "List of AI providers. Supports Claude, Gemini, Continue, Opencode, Copilot, Mammouth. Replaces the deprecated ai-provider string field. Backward-compatible: ai-provider (string) still works.",
      "items": {
        "type": "string",
        "enum": [
          "Claude",
          "Gemini",
          "Continue",
          "Opencode",
          "Copilot",
          "Mammouth"
        ]
      },
      "uniqueItems": true
    },
    "platforms": {
      "type": "array",
      "description": "Active platform layers. Agents in agents/2-platform/<platform>-*.md override the generic agents.",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    },
    "roles": {
      "type": "array",
      "description": "Whitelist of roles to generate. If absent, all known roles are generated.",
      "items": {
        "type": "string",
        "enum": [
          "accessibility-specialist",
          "agent-meta-manager",
          "agent-meta-scout",
          "ai-security-guardian",
          "api-specialist",
          "app-lifecycle-governor",
          "backend-reviewer",
          "bug-feature-analyzer",
          "claude-expert",
          "code-reviewer",
          "concept-reviewer",
          "continue-expert",
          "copilot-expert",
          "copyeditor",
          "data-engineer",
          "database-engineer",
          "database-reviewer",
          "dependency-auditor",
          "design-system-architect",
          "developer",
          "devops-engineer",
          "docker",
          "documenter",
          "e2e-tester",
          "effort-estimator",
          "explorer",
          "export-manager",
          "feedback",
          "frontend-component-engineer",
          "frontend-reviewer",
          "gemini-expert",
          "git",
          "ideation",
          "incident-responder",
          "intern-developer",
          "junior-developer",
          "knowledge-curator",
          "knowledge-gardener",
          "knowledge-indexer",
          "knowledge-ingestor",
          "knowledge-linter",
          "knowledge-migrator",
          "knowledge-querier",
          "log-analyzer",
          "mammouth-expert",
          "meta-feedback",
          "opencode-expert",
          "openscad-developer",
          "orchestrator",
          "performance-optimizer",
          "planner",
          "principal-developer",
          "product-manager",
          "prompt-engineer",
          "prompt-governor",
          "proofreader",
          "refactoring-specialist",
          "release",
          "requirements",
          "se-architect",
          "se-critic",
          "se-developer",
          "se-integration-and-test-manager",
          "se-interface-mgr",
          "se-junior-developer",
          "se-requirements",
          "se-senior-developer",
          "se-termination",
          "se-test-engineer",
          "se-testreviewer",
          "se-validator",
          "se-verifier",
          "security-auditor",
          "senior-developer",
          "sre-engineer",
          "technical-writer",
          "test-executor",
          "tester",
          "ui-reviewer",
          "ui-ux-designer",
          "validator"
        ],
        "$comment": "Auto-generated by sync.py from config/role-defaults.yaml — do not edit manually"
      },
      "uniqueItems": true
    },
    "project": {
      "type": "object",
      "description": "Project identity — used in agent filenames and managed blocks.",
      "properties": {
        "name": {
          "type": "string",
          "description": "Full project name (e.g. 'my-plugin')."
        },
        "prefix": {
          "type": "string",
          "description": "Short prefix for extension filenames (e.g. 'mpl' → mpl-developer-ext.md)."
        },
        "short": {
          "type": "string",
          "description": "Short project name for display (e.g. 'my-plugin')."
        }
      },
      "required": [
        "name",
        "prefix",
        "short"
      ],
      "additionalProperties": false
    },
    "variables": {
      "type": "object",
      "description": "Template variables substituted as {{KEY}} in agent templates. Non-string scalars (booleans, numbers) are coerced to strings during sync.",
      "properties": {
        "PROJECT_CONTEXT": {
          "type": "string"
        },
        "PROJECT_GOAL": {
          "type": "string"
        },
        "PROJECT_LANGUAGES": {
          "type": "string"
        },
        "COMMUNICATION_LANGUAGE": {
          "type": "string"
        },
        "USER_INPUT_LANGUAGE": {
          "type": "string"
        },
        "DOCS_LANGUAGE": {
          "type": "string"
        },
        "INTERNAL_DOCS_LANGUAGE": {
          "type": "string"
        },
        "CODE_LANGUAGE": {
          "type": "string"
        },
        "AGENT_META_REPO": {
          "type": "string"
        },
        "AGENTS_DIR": {
          "type": "string",
          "description": "Provider-agnostic path to generated agents directory. Default: .claude/agents"
        },
        "GIT_PLATFORM": {
          "type": "string",
          "enum": [
            "GitHub",
            "GitLab",
            "Gitea",
            "Codeberg"
          ]
        },
        "GIT_REMOTE_URL": {
          "type": "string"
        },
        "GIT_MAIN_BRANCH": {
          "type": "string"
        },
        "CODE_CONVENTIONS": {
          "type": "string"
        },
        "ARCHITECTURE": {
          "type": "string"
        },
        "DEV_COMMANDS": {
          "type": "string"
        },
        "BUILD_COMMAND": {
          "type": "string"
        },
        "TEST_COMMAND": {
          "type": "string"
        },
        "EXTRA_DONTS": {
          "type": "string"
        },
        "CODE_QUALITY_RULES": {
          "type": "string"
        },
        "REQ_CATEGORIES": {
          "type": "string"
        },
        "TEST_COMMANDS": {
          "type": "string"
        },
        "TESTER_SNIPPETS_PATH": {
          "type": "string"
        },
        "DEVELOPER_SNIPPETS_PATH": {
          "type": "string"
        },
        "BUILD_COMMANDS": {
          "type": "string"
        },
        "SYSTEM_DEPENDENCIES": {
          "type": "string"
        },
        "SYSTEM_URLS": {
          "type": "string"
        },
        "PRIMARY_PORT": {
          "type": "string"
        },
        "EXTRA_PORTS": {
          "type": "string"
        },
        "SERVICE_NAME": {
          "type": "string"
        },
        "CONTAINER_NAME": {
          "type": "string"
        },
        "PRIMARY_IMAGE_TAG": {
          "type": "string"
        },
        "PLUGIN_DIR_NAME": {
          "type": "string"
        },
        "BUILD_OUTPUT": {
          "type": "string"
        },
        "ARTIFACT_ZIP_CMD": {
          "type": "string"
        },
        "ARTIFACT_TAR_CMD": {
          "type": "string"
        },
        "GH_ASSETS": {
          "type": "string"
        },
        "BUILD_SYSTEM_NOTES": {
          "type": "string"
        },
        "VERSION_DIST_BEHAVIOUR": {
          "type": "string"
        }
      },
      "additionalProperties": {
        "type": [
          "string",
          "boolean",
          "integer",
          "number"
        ]
      }
    },
    "model-override-all": {
      "type": "object",
      "description": "Global per-provider model blast. When a provider key is set (non-empty), EVERY active agent/role of that provider resolves to this single model during sync.py — overriding all per-role, tier, preset and alias resolution. Fully reversible: remove the key (or the whole block) and the previous per-agent settings apply again automatically. Value may be a tier (nano/fast/balanced/powerful/max), a legacy alias (haiku/sonnet/opus) or a full model ID (e.g. claude-sonnet-4-6).",
      "additionalProperties": {
        "type": "string",
        "description": "Tier, alias or full model ID to blast onto every role of this provider."
      }
    },
    "model-inherit-main-chat": {
      "type": "object",
      "description": "Per-provider main-chat inheritance. When a provider key is set to true, agents of that provider get no model field during sync.py and inherit the main chat model at runtime — overriding all per-role overrides, tiers and presets. Mutually exclusive with model-override-all on the same provider key. Fully reversible: remove the key (or the whole block) and the previous per-agent settings apply again automatically.",
      "additionalProperties": {
        "type": "boolean",
        "description": "Set to true to omit the model field for every agent of this provider so it inherits the main chat model."
      }
    },
    "model-overrides": {
      "type": "object",
      "description": "Override the default model tier or model ID per role. Two forms supported: (1) flat role→tier for Claude-only legacy use; (2) provider-keyed blocks for multi-provider control. Provider-specific blocks take precedence over flat values for that provider.",
      "properties": {
        "Claude": {
          "type": "object",
          "description": "Claude-specific model overrides: role → tier or full model ID.",
          "additionalProperties": {
            "type": "string",
            "anyOf": [
              {
                "enum": [
                  "nano",
                  "fast",
                  "balanced",
                  "powerful",
                  "max"
                ]
              },
              {
                "enum": [
                  "haiku",
                  "sonnet",
                  "opus"
                ]
              },
              {
                "pattern": "^claude-"
              }
            ]
          }
        },
        "Gemini": {
          "type": "object",
          "description": "Gemini-specific model overrides: role → tier or full Gemini model ID.",
          "additionalProperties": {
            "type": "string",
            "anyOf": [
              {
                "enum": [
                  "nano",
                  "fast",
                  "balanced",
                  "powerful",
                  "max"
                ]
              },
              {
                "pattern": "^gemini-"
              }
            ]
          }
        },
        "Continue": {
          "type": "object",
          "description": "Continue model overrides (ignored — Continue manages models in config.yaml).",
          "additionalProperties": {
            "type": "string"
          }
        },
        "Opencode": {
          "type": "object",
          "description": "Opencode model overrides (Opencode manages models in opencode.json).",
          "additionalProperties": {
            "type": "string"
          }
        }
      },
      "additionalProperties": {
        "type": "string",
        "description": "Legacy flat form: role → tier/alias/model-id. Applied to Claude only.",
        "anyOf": [
          {
            "enum": [
              "nano",
              "fast",
              "balanced",
              "powerful",
              "max"
            ]
          },
          {
            "enum": [
              "haiku",
              "sonnet",
              "opus"
            ]
          },
          {
            "pattern": "^claude-"
          },
          {
            "pattern": "^gemini-"
          }
        ]
      }
    },
    "memory": {
      "description": "Global memory configuration. Controls whether memory fields are generated for agents.",
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Master kill-switch: false = suppress ALL memory: fields during sync (overrides role-defaults and memory-overrides)",
          "default": true
        },
        "default_scope": {
          "type": "string",
          "enum": [
            "",
            "project",
            "local",
            "user"
          ],
          "description": "Default memory scope for roles that have no explicit memory: in role-defaults.yaml and no entry in memory-overrides. Empty string = no memory injected.",
          "default": ""
        }
      },
      "additionalProperties": false
    },
    "memory-overrides": {
      "type": "object",
      "description": "Override the default memory scope per role. Precedence: this > roles.config.json > (none).",
      "additionalProperties": {
        "type": "string",
        "enum": [
          "project",
          "local",
          "user"
        ]
      }
    },
    "mcp-role-overrides": {
      "type": "object",
      "description": "Override which MCP servers a role gets tools for. Precedence: this > role-defaults.yaml mcp-servers > (none). Only servers active for the project contribute tools; an empty list removes them.",
      "additionalProperties": {
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    },
    "permission-mode-overrides": {
      "type": "object",
      "description": "Override the default permissionMode per role. Precedence: this > roles.config.json > (none).",
      "additionalProperties": {
        "type": "string",
        "enum": [
          "default",
          "acceptEdits",
          "bypassPermissions",
          "plan"
        ]
      }
    },
    "temperature-overrides": {
      "type": "object",
      "description": "Override sampling temperature per role. Float 0.0–1.0.",
      "additionalProperties": {
        "type": "number",
        "minimum": 0.0,
        "maximum": 1.0
      }
    },
    "tier-preset": {
      "type": "string",
      "description": "The active tier preset mapped against config/tier-presets.yaml. Adjusts model selection globally. Use se-focus: true instead of the deprecated '(SE)' suffix variants.",
      "enum": [
        "Cheap",
        "Normal",
        "Advanced",
        "Expensive",
        "Expensive as Hell"
      ],
      "default": "Normal"
    },
    "se-focus": {
      "type": "boolean",
      "default": false,
      "description": "When true, SE roles (se-architect, se-critic, se-developer, etc.) are upgraded one tier before preset mapping is applied. Replaces the deprecated '(SE)' suffix in tier-preset names."
    },
    "tier-overrides": {
      "type": "object",
      "description": "Override the resolved tier per role. Value is a tier name (nano/fast/balanced/powerful/max/ultra) or a raw model-id string (e.g. claude-sonnet-5).",
      "additionalProperties": {
        "type": "string"
      }
    },
    "provider-tier-overrides": {
      "type": "object",
      "description": "Override the resolved tier per role for a specific provider. Keys are provider names (Claude, Gemini, Opencode, Continue, Copilot, Mammouth). Values are objects mapping role → tier name or model-id string.",
      "additionalProperties": {
        "type": "object",
        "description": "Role-to-tier overrides for this provider. Value is a tier name or a raw model-id string.",
        "additionalProperties": {
          "type": "string"
        }
      }
    },
    "model-source-preference": {
      "type": "object",
      "description": "Per-provider override of the model source that drives every model dropdown/suggestion in the Admin UI. Keys are provider names (Claude, Gemini, Opencode, Copilot, Mammouth, Continue). Providers without an entry here fall back to the central 'default-model-source' in config/ai-providers.yaml (itself defaulting to 'registry' if unset).",
      "additionalProperties": {
        "type": "string",
        "enum": [
          "registry",
          "modelsdev"
        ]
      }
    },
    "max-tokens-overrides": {
      "type": "object",
      "description": "Override max output tokens per role. Positive integer.",
      "additionalProperties": {
        "type": "integer",
        "minimum": 1
      }
    },
    "dod-preset": {
      "type": "string",
      "description": "DoD quality profile. Sets defaults for all dod criteria. Individual overrides via 'dod' block. Presets defined in dod-presets.config.json.",
      "enum": [
        "full",
        "standard",
        "rapid-prototyping",
        "spec-optional",
        "spec-driven",
        "spec-certified"
      ],
      "default": "full"
    },
    "dod": {
      "type": "object",
      "description": "Definition of Done — override individual criteria from the preset. Precedence: dod > dod-preset > full.",
      "properties": {
        "req-traceability": {
          "type": "boolean",
          "description": "Require REQ-IDs in REQUIREMENTS.md, REQ-ID in commit messages, traceability audit. Default: true.",
          "default": true
        },
        "tests-required": {
          "type": "boolean",
          "description": "Require tests for every code change, TDD workflow steps. Default: true.",
          "default": true
        },
        "codebase-overview": {
          "type": "boolean",
          "description": "Require CODEBASE_OVERVIEW.md updates after code changes. Default: true.",
          "default": true
        },
        "security-audit": {
          "type": "boolean",
          "description": "Require security audit before release. Default: false.",
          "default": false
        },
        "ai-security-review": {
          "type": "boolean",
          "description": "Require AI-specific security review of AI-generated code (ai-security-guardian). Default: false.",
          "default": false
        },
        "prompt-governance": {
          "type": "boolean",
          "description": "Require prompts to be governed as source code (prompt-governor). Default: false.",
          "default": false
        },
        "lifecycle-ownership": {
          "type": "boolean",
          "description": "Require a named owner and deprecation plan for every app/service (app-lifecycle-governor). Default: false.",
          "default": false
        },
        "se-required": {
          "type": "string",
          "description": "SE cascade binding strength. 'false' = optional, 'recommended' = advised for complex features, 'true' = mandatory before any code. Default: false.",
          "enum": [
            "false",
            "recommended",
            "true"
          ],
          "default": "false"
        }
      },
      "additionalProperties": false
    },
    "release-gates": {
      "type": "object",
      "description": "Configure the pre-release-check.sh gate scripts (hooks/*/release-gates/*.sh, issue #558) — a plugin-style dispatcher, not a fixed enum. Keys are gate script file stems: the three built-ins ('artifact-freshness', 'docker-image-scan', 'action-pin-validation', defaulted per dod-preset — see config/dod-presets.yaml) plus any project-authored custom gate dropped into <hooks_dir>/release-gates/. Precedence: this block (highest) > dod-preset default > the gate script's own 'enabled_by_default' header.",
      "additionalProperties": {
        "description": "Per-gate config. Either a bare boolean shorthand for 'enabled', or an object form. 'enabled' is read by sync.py to bake the gate script's default; any other object keys are opaque to the framework — reserved for that specific gate script to read itself (env var, own project.yaml parsing, etc.).",
        "oneOf": [
          {
            "type": "boolean",
            "description": "Shorthand for {enabled: <this value>}."
          },
          {
            "type": "object",
            "properties": {
              "enabled": {
                "type": "boolean",
                "description": "Enable/disable this gate, overriding the dod-preset default."
              }
            },
            "additionalProperties": true
          }
        ]
      }
    },
    "conventions-preset": {
      "type": "string",
      "description": "The active naming/versioning conventions preset, matched against config/conventions-presets.yaml. Individual overrides via 'conventions' block. Precedence: conventions > conventions-preset > 'default'.",
      "default": "default"
    },
    "conventions": {
      "type": "object",
      "description": "Override individual naming/versioning convention fields on top of the selected conventions-preset. Structure mirrors config/conventions-presets.yaml presets.<name> (release.versioning, release.changelog, issues.*). 'issues.language' (default: 'english') sets the language for GitHub issues the feedback agent creates for this project — free-form string, not enum-constrained, same as every other nested conventions field (e.g. 'deutsch', 'français'); meta-feedback is unaffected and always stays English.",
      "additionalProperties": true
    },
    "debug-mode": {
      "type": "boolean",
      "description": "When true, sync.py injects a debug block into every generated agent. Active agents announce themselves ([Agent: <name>]), log every delegation (→ Delegiere an: ...), and confirm completion (✓ [Agent: <name>] fertig). Default: false — no change to any agent file.",
      "default": false
    },
    "max-parallel-agents": {
      "type": "integer",
      "description": "Maximum number of agents that a coordinator (orchestrator, feature) may run in parallel via run_in_background. Default: 2. Set to 1 to disable parallelism.",
      "minimum": 1,
      "maximum": 5,
      "default": 2
    },
    "speech-mode": {
      "type": "string",
      "description": "Communication style for all agents. Copied as .claude/rules/speech-mode.md — loaded automatically by Claude Code into every agent context. 'full' = default behavior, no rule generated. 'short' = facts only, no filler. 'childish' = playful, simple language with animal/toy analogies. 'caveman' = brutally short, caveman speech. 'asozial' = technically correct but dripping with contempt — New Kids style, insults the user's intelligence. 'submissive' = completely devoted and submissive, no contradiction, no own opinion, addresses user as master/mistress. 'business' = formal corporate tone. 'bullshit-bingo' = management speak, buzzwords. 'linkedin' = LinkedIn influencer style, inspirational.",
      "enum": [
        "full",
        "short",
        "childish",
        "caveman",
        "asozial",
        "submissive",
        "business",
        "bullshit-bingo",
        "linkedin"
      ],
      "default": "full"
    },
    "hooks": {
      "type": "object",
      "description": "Opt-in hook activation. Scripts are always synced; registration in settings.json requires enabled: true.",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "enabled": {
            "type": "boolean",
            "description": "Register this hook in .claude/settings.json (Two-Gate: script must exist in hooks/ layer)."
          }
        },
        "required": [
          "enabled"
        ],
        "additionalProperties": false
      }
    },
    "provider-options": {
      "type": "object",
      "description": "Provider-specific options. Each key is a provider name (Claude, Gemini, Continue, Opencode, Copilot, Mammouth).",
      "properties": {
        "Claude": {
          "type": "object",
          "description": "Options for the Claude provider.",
          "additionalProperties": false,
          "properties": {}
        },
        "Gemini": {
          "type": "object",
          "description": "Options for the Gemini provider.",
          "additionalProperties": false,
          "properties": {}
        },
        "Continue": {
          "type": "object",
          "description": "Options for the Continue provider.",
          "additionalProperties": false,
          "properties": {
            "generate-prompts": {
              "type": "boolean",
              "description": "Generate .continue/prompts/<role>.md as invokable slash-commands. Works with any local LLM — no tool calling required."
            },
            "prompt-mode": {
              "type": "string",
              "description": "Content mode for generated prompts. 'full' = complete agent body. 'slim' = compact role + core rules only (~80 lines).",
              "enum": [
                "full",
                "slim"
              ],
              "default": "full"
            }
          }
        },
        "Opencode": {
          "type": "object",
          "description": "Options for the Opencode provider.",
          "additionalProperties": false,
          "properties": {}
        },
        "Mammouth": {
          "type": "object",
          "description": "Options for the Mammouth Code provider.",
          "additionalProperties": false,
          "properties": {}
        }
      },
      "additionalProperties": true
    },
    "gitignore": {
      "type": "object",
      "description": "Configurable .gitignore behavior for generated and settings files. Defaults match current behavior (local: true, generated: false, settings: false).",
      "properties": {
        "local": {
          "type": "boolean",
          "default": true,
          "description": "Gitignore local/personal files (.claude/settings.local.json, CLAUDE.personal.md, sync.log). Default: true (gitignored)."
        },
        "generated": {
          "type": "boolean",
          "default": false,
          "description": "Gitignore all generated directories (agents/, rules/, hooks/, commands/) for all active providers. Default: false (committed). Set true to follow 'generate, don't store' principle."
        },
        "settings": {
          "type": "boolean",
          "default": false,
          "description": "Gitignore provider settings files (settings.json, GEMINI.md, config.yaml) for all active providers. Default: false (committed). Note: CLAUDE.md is never gitignored (contains handwritten sections)."
        },
        "ignore-provider-dirs": {
          "type": "boolean",
          "default": false,
          "description": "Issue #557: switch the agent-meta managed .gitignore block from the provider-internal sub-path allowlist to whole provider-root directories (e.g. .claude/, .gemini/, .github/copilot/), resolved per provider from config/ai-providers.yaml (provider_root_dirs key; fallback derivation: first dot-prefixed segment of agents_dir). Provider-internal entries from the local/generated/settings categories are filtered out as redundant. Repo-root files (sync.log, CLAUDE.personal.md, .mcp.json, opencode.json, top-level context files) keep their per-category behavior, as do non-provider entries (env scripts, MCP extras, skill entries). Default: false (sub-path allowlist, unchanged behavior). Note: the managed block is additive on non-exact sync paths — existing projects may keep previously written sub-path entries (harmless, redundant)."
        },
        "custom_entries": {
          "type": "array",
          "description": "List of custom paths (files or directories) to add to the agent-meta managed .gitignore block.",
          "items": {
            "type": "string"
          }
        },
        "exceptions": {
          "type": "array",
          "description": "List of specific paths that should bypass their category's default rule.",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "context_file": {
      "type": "object",
      "description": "Generated provider context files (CLAUDE.md, AGENTS.md, ...) behavior. Missing keys fall back to the documented defaults.",
      "properties": {
        "mode": {
          "type": "string",
          "description": "Context file density. 'full' = complete managed block with all overviews (current behavior). 'compact' = instruction-preserving compressed output (issue #540); overviews and reference prose are dropped in favor of pointers. Missing or invalid value always resolves to 'full' (safe-side default: compression must be explicitly opted in).",
          "enum": [
            "full",
            "compact"
          ],
          "default": "full"
        },
        "max_lines": {
          "type": "integer",
          "description": "Soft size limit for generated provider context files. The consistency check (sync.py --validate / consistency-check.py) emits a WARNING when a generated file exceeds this many lines without context_file.oversize_acknowledged. Default: 250.",
          "default": 250,
          "minimum": 1
        },
        "oversize_acknowledged": {
          "type": "boolean",
          "description": "Explicit acknowledgment that oversized generated context files are accepted. When true, the >max_lines size warning is suppressed for all providers. Default: false.",
          "default": false
        },
        "auto_generate": {
          "type": "boolean",
          "description": "Declares whether provider context files may be regenerated automatically on every sync. Recommended: false for newly scaffolded projects (regenerate deliberately instead of on every run). Currently declarative only — sync.py still regenerates managed blocks unconditionally; enforcement is planned as issue #540 Fix 5.",
          "default": true
        }
      },
      "additionalProperties": false
    },
    "lifecycle-triggers": {
      "type": "object",
      "description": "Agent tasks to run automatically on git lifecycle events (commit, merge, version bump, release). Requires lifecycle-check hook to be enabled.",
      "properties": {
        "on-commit": {
          "$ref": "#/$defs/lifecycleTriggerList"
        },
        "on-merge": {
          "$ref": "#/$defs/lifecycleTriggerList"
        },
        "on-version-bump-patch": {
          "$ref": "#/$defs/lifecycleTriggerList"
        },
        "on-version-bump-minor": {
          "$ref": "#/$defs/lifecycleTriggerList"
        },
        "on-version-bump-major": {
          "$ref": "#/$defs/lifecycleTriggerList"
        },
        "on-release": {
          "$ref": "#/$defs/lifecycleTriggerList"
        },
        "on-config-change": {
          "$ref": "#/$defs/lifecycleTriggerList"
        }
      },
      "additionalProperties": false
    },
    "mcp-servers": {
      "type": "array",
      "description": "DEPRECATED — legacy MCP-server activation list, read only for projects not yet migrated to the unified `plugins` block. Use `plugins` instead: MCP servers are the mcp-server kind of config/plugin-catalog.yaml. Servers listed here are always active; additional servers may be added implicitly via platform bundles (rules/2-platform/<platform>-mcp.yaml) when enabled-by-default: true.",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    },
    "mcp-registry": {
      "type": "object",
      "description": "DEPRECATED — no longer read. MCP-server definitions live in config/plugin-catalog.yaml (mcp-server kind); per-project overrides go in `.meta-config/plugin-catalog.yaml` or the inline `plugin-catalog` key. Retained only to document the (now unified) override shape.",
      "patternProperties": {
        "^[a-zA-Z0-9_-]+$": {
          "type": "object",
          "properties": {
            "description": {
              "type": "string"
            },
            "category": {
              "type": "string"
            },
            "enabled-by-default": {
              "type": "boolean"
            },
            "tools": {
              "type": "object",
              "properties": {
                "allowed": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "blocked": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              }
            },
            "agent-hint": {
              "type": "string"
            },
            "connection": {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string"
                },
                "url": {
                  "type": "string"
                },
                "command": {
                  "type": "string"
                },
                "args": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "env": {
                  "type": "object"
                },
                "headers": {
                  "type": "object"
                }
              }
            },
            "secrets": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "provider-skip": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        }
      }
    },
    "plugins": {
      "description": "Unified activation of catalogued plugins (config/plugin-catalog.yaml) — both MCP servers (kind: mcp-server) and CLI tools (kind: cli-tool). Supersedes the legacy `mcp-servers` and `external-tools` keys. Accepts a flat list shorthand (['graphify'], alias for {enabled: true}) or a dict mapping plugin id to {enabled: bool}. An explicit enabled value always wins over the catalog enabled-by-default flag; MCP servers may additionally be activated implicitly via platform bundles.",
      "oneOf": [
        {
          "type": "array",
          "items": {
            "type": "string"
          },
          "uniqueItems": true
        },
        {
          "type": "object",
          "patternProperties": {
            "^[a-zA-Z0-9_-]+$": {
              "type": "object",
              "properties": {
                "enabled": {
                  "type": "boolean",
                  "description": "Force-enable or force-disable this plugin, overriding the catalog enabled-by-default flag."
                }
              },
              "required": [
                "enabled"
              ],
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "plugin-catalog": {
      "type": "object",
      "description": "Inline per-project overrides / custom definitions for the unified plugin catalog (config/plugin-catalog.yaml). Deep-merged over the framework catalog and the `.meta-config/plugin-catalog.yaml` project layer. Each entry is discriminated by `kind` (mcp-server or cli-tool) and follows the same shape as the framework catalog; only the fields you override need to be present. Prefer editing `.meta-config/plugin-catalog.yaml` (via the Admin UI Plugin Overrides panel) over inlining here.",
      "properties": {
        "plugins": {
          "type": "object",
          "description": "Map of plugin id to a (partial) plugin definition merged over the framework catalog.",
          "patternProperties": {
            "^[a-zA-Z0-9_-]+$": {
              "type": "object",
              "properties": {
                "kind": {
                  "type": "string",
                  "enum": [
                    "mcp-server",
                    "cli-tool"
                  ]
                },
                "description": {
                  "type": "string"
                },
                "category": {
                  "type": "string"
                },
                "enabled-by-default": {
                  "type": "boolean"
                },
                "connection": {
                  "type": "object",
                  "description": "mcp-server only.",
                  "properties": {
                    "type": {
                      "type": "string"
                    },
                    "url": {
                      "type": "string"
                    },
                    "command": {
                      "type": "string"
                    },
                    "args": {
                      "type": "array",
                      "items": {
                        "type": "string"
                      }
                    },
                    "env": {
                      "type": "object"
                    },
                    "headers": {
                      "type": "object"
                    }
                  }
                },
                "tools": {
                  "type": "object",
                  "description": "mcp-server only.",
                  "properties": {
                    "allowed": {
                      "type": "array",
                      "items": {
                        "type": "string"
                      }
                    },
                    "blocked": {
                      "type": "array",
                      "items": {
                        "type": "string"
                      }
                    }
                  }
                },
                "secrets": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "agent-hint": {
                  "type": "string"
                },
                "rule-content": {
                  "type": "string",
                  "description": "cli-tool only."
                },
                "hooks": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "cli-tool only."
                },
                "provider-skip": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "permitted-injections": {
                  "type": "array",
                  "description": "cli-tool only.",
                  "items": {
                    "type": "object",
                    "properties": {
                      "kind": {
                        "type": "string",
                        "enum": [
                          "skill",
                          "hook",
                          "rule",
                          "config",
                          "other"
                        ]
                      },
                      "name": {
                        "type": "string"
                      },
                      "path": {
                        "type": "string"
                      },
                      "description": {
                        "type": "string"
                      }
                    },
                    "required": [
                      "kind"
                    ],
                    "additionalProperties": false
                  }
                }
              }
            }
          }
        }
      }
    },
    "provider-isolation": {
      "type": "string",
      "enum": [
        "disabled"
      ],
      "description": "Set to 'disabled' to skip provider isolation generation (e.g. in the agent-meta meta-repo itself, which intentionally manages all provider directories)."
    },
    "allow-committed-secrets": {
      "type": "boolean",
      "description": "Allow sync.py to write files containing detected secrets to committed (non-gitignored) paths. Default: false — sync aborts with an error when a secret is detected in a committed file. Set to true only when you deliberately commit secret-looking values (e.g. static demo tokens). NOT recommended for production projects.",
      "default": false
    },
    "outcome-caching": {
      "$ref": "#/$defs/outcome-caching"
    },
    "cascades": {
      "description": "SE-Kaskaden-Definitionen (Phase 3+). First-Class neben quality-pipelines.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/cascadeDefinition"
      }
    },
    "external-skills": {
      "type": "object",
      "description": "Opt-in external skill activation. Two-Gate: approved in external-skills.config.json AND enabled here.",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "enabled": {
            "type": "boolean",
            "description": "Generate wrapper agent for this skill."
          },
          "gitignore": {
            "type": "boolean",
            "description": "Add .claude/skills/<skill-name>/ to the .gitignore managed block. Use when the skill copies large or generated files that should not be committed."
          }
        },
        "required": [
          "enabled"
        ],
        "additionalProperties": false
      }
    },
    "external-tools": {
      "description": "DEPRECATED — legacy CLI-tool activation, read only for projects not yet migrated to the unified `plugins` block. Use `plugins` instead: CLI tools are the cli-tool kind of config/plugin-catalog.yaml. Accepts a flat list shorthand (['graphify'], alias for {enabled: true}) or a dict mapping tool name to {enabled: bool}; an explicit enabled value always wins over the catalog enabled-by-default flag.",
      "oneOf": [
        {
          "type": "array",
          "items": {
            "type": "string"
          },
          "uniqueItems": true
        },
        {
          "type": "object",
          "patternProperties": {
            "^[a-zA-Z0-9_-]+$": {
              "type": "object",
              "properties": {
                "enabled": {
                  "type": "boolean",
                  "description": "Force-enable or force-disable this external tool, overriding the registry enabled-by-default flag."
                }
              },
              "required": [
                "enabled"
              ],
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "external-tools-registry": {
      "type": "object",
      "description": "DEPRECATED — no longer read. CLI-tool definitions live in config/plugin-catalog.yaml (cli-tool kind); per-project overrides go in `.meta-config/plugin-catalog.yaml` or the inline `plugin-catalog` key. Retained only to document the (now unified) override shape.",
      "patternProperties": {
        "^[a-zA-Z0-9_-]+$": {
          "type": "object",
          "properties": {
            "description": {
              "type": "string"
            },
            "category": {
              "type": "string"
            },
            "enabled-by-default": {
              "type": "boolean"
            },
            "provider-skip": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "rule-content": {
              "type": "string"
            },
            "hooks": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "permitted-injections": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "kind": {
                    "type": "string",
                    "enum": [
                      "skill",
                      "hook",
                      "rule",
                      "config",
                      "other"
                    ]
                  },
                  "name": {
                    "type": "string"
                  },
                  "path": {
                    "type": "string"
                  },
                  "description": {
                    "type": "string"
                  }
                },
                "required": [
                  "kind"
                ],
                "additionalProperties": false
              }
            }
          }
        }
      }
    },
    "quality-pipelines": {
      "type": "object",
      "properties": {
        "overrides": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/pipelineDefinition"
          }
        },
        "custom-pipelines": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/pipelineDefinition"
          }
        }
      }
    },
    "reflection-pairs": {
      "type": "object",
      "description": "Project-level overrides for reflection pairs (generator/critic loops) defined in config/role-defaults.yaml. Each key under 'overrides' is a reflection pair id (e.g. 'dev-review-loop'); its value is merged into the base pair definition.",
      "properties": {
        "overrides": {
          "type": "object",
          "description": "Map of reflection pair id to override fields. Merged on top of the base pair from role-defaults.yaml.",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "enabled": {
                "type": "boolean",
                "description": "Enable or disable this reflection pair for the project."
              },
              "max_iterations": {
                "type": "integer",
                "minimum": 1,
                "description": "Maximum number of generator/critic iterations before the loop stops."
              },
              "generator": {
                "type": "string",
                "description": "Role that produces output in the loop."
              },
              "critic": {
                "type": "string",
                "description": "Role that reviews the generator's output."
              }
            }
          }
        }
      }
    },
    "admin-ui": {
      "type": "object",
      "description": "Admin UI server configuration. Controls the optional zero-dependency HTTP server that serves docs/ui/admin-ui.html and exposes REST endpoints for the YAML configuration files.",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable Admin UI server (start via 'python scripts/sync.py --admin' or '--admin-only')."
        },
        "port": {
          "type": "integer",
          "default": 7420,
          "minimum": 1024,
          "maximum": 65535,
          "description": "TCP port the Admin UI server binds to (localhost only)."
        },
        "auto-sync-on-save": {
          "type": "boolean",
          "default": false,
          "description": "Automatically run sync.py --validate after every config save via the UI."
        },
        "watch-config": {
          "type": "boolean",
          "default": false,
          "description": "Watch config files for external modifications and emit events to .meta-viz/events.jsonl."
        },
        "bind-host": {
          "type": "string",
          "default": "127.0.0.1",
          "description": "IP address the Admin UI HTTP server binds to. Default: 127.0.0.1 (loopback only). Set to 0.0.0.0 for network access — requires token authentication."
        },
        "token": {
          "type": "string",
          "description": "Authentication token for remote access. REQUIRED when bind-host is non-loopback. Equivalent to --admin-token CLI flag or ADMIN_UI_TOKEN env var. NEVER commit to version control."
        },
        "token-file": {
          "type": "string",
          "description": "Path to a file containing the admin auth token. Alternative to inline 'token' key for secret management (e.g., Docker secrets, 1Password CLI)."
        },
        "allowed-hosts": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "127.0.0.1",
            "localhost",
            "::1"
          ],
          "description": "List of host:port origins allowed for CORS / DNS-rebinding protection. Extends the default loopback set. Use when binding to a specific network interface."
        }
      },
      "additionalProperties": true
    },
    "knowledge-engine": {
      "type": "object",
      "description": "Knowledge Engine — opt-in bundle for structured, queryable project/domain knowledge (Phase A: activation + scaffolding only, no agents yet).",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable the Knowledge Engine. When true, sync.py scaffolds the bundle-path directory on first run."
        },
        "domain": {
          "type": "string",
          "enum": [
            "research",
            "personal",
            "business",
            "book",
            "internal-docs",
            "technical",
            "custom"
          ],
          "default": "research",
          "description": "Concept-type preset for schema.md generation. See scripts/lib/knowledge.py DOMAIN_CONCEPT_TYPES."
        },
        "bundle-path": {
          "type": "string",
          "default": "knowledge",
          "description": "Root directory of the knowledge bundle, relative to the project root."
        },
        "sources-dir": {
          "type": "string",
          "default": "sources",
          "description": "Relative path (inside the bundle) where raw source documents live before ingestion."
        },
        "wiki-dir": {
          "type": "string",
          "default": "wiki",
          "description": "Relative path (inside the bundle) where curated wiki pages are written."
        },
        "schema-language": {
          "type": "string",
          "default": "auto",
          "description": "Language used for schema.md and generated wiki page headers ('auto' = follow project language rules)."
        },
        "okf": {
          "type": "object",
          "description": "Open Knowledge Format enforcement rules for ingested/curated pages.",
          "properties": {
            "enforce-frontmatter": {
              "type": "boolean",
              "default": true
            },
            "allowed-types": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "default": []
            },
            "auto-index": {
              "type": "boolean",
              "default": true
            },
            "auto-log": {
              "type": "boolean",
              "default": true
            }
          },
          "additionalProperties": false
        },
        "operations": {
          "type": "object",
          "description": "Behavior toggles for the ingest, query and lint pipelines.",
          "properties": {
            "ingest": {
              "type": "object",
              "properties": {
                "auto-cross-reference": {
                  "type": "boolean",
                  "default": true
                },
                "auto-index-update": {
                  "type": "boolean",
                  "default": true
                },
                "batch-mode": {
                  "type": "boolean",
                  "default": false
                }
              },
              "additionalProperties": false
            },
            "query": {
              "type": "object",
              "properties": {
                "file-back-results": {
                  "type": "boolean",
                  "default": true
                }
              },
              "additionalProperties": false
            },
            "lint": {
              "type": "object",
              "properties": {
                "schedule": {
                  "type": "string",
                  "enum": [
                    "on-demand",
                    "post-ingest",
                    "periodic"
                  ],
                  "default": "on-demand"
                },
                "checks": {
                  "type": "array",
                  "items": {
                    "type": "string",
                    "enum": [
                      "broken-links",
                      "missing-frontmatter",
                      "orphaned-pages",
                      "duplicate-concepts",
                      "stale-index",
                      "inconsistent-naming",
                      "missing-citations",
                      "unresolved-todos"
                    ]
                  },
                  "default": [
                    "broken-links",
                    "missing-frontmatter"
                  ]
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        },
        "migration": {
          "type": "object",
          "description": "Safety toggles for the knowledge-migrator agent. Migration copies, never moves.",
          "properties": {
            "auto-detect-sources": {
              "type": "boolean",
              "default": false
            },
            "clean-duplicates": {
              "type": "boolean",
              "default": false
            },
            "preserve-originals": {
              "type": "boolean",
              "default": true
            }
          },
          "additionalProperties": false
        },
        "search": {
          "type": "object",
          "properties": {
            "engine": {
              "type": "string",
              "enum": [
                "index-only",
                "mcp-qmd",
                "custom"
              ],
              "default": "index-only"
            },
            "mcp-server": {
              "type": "string",
              "default": ""
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "viz": {
      "type": "object",
      "description": "Visualization / session tracking configuration. Controls the Viz dashboard (viz-report.py) and the MCP SSE server (viz-logger.py) that the Admin UI supervises.",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable the viz dashboard and MCP SSE server subprocesses."
        },
        "mode": {
          "type": "string",
          "default": "off",
          "description": "Viz mode. 'off' = no event emission; other values reserved for future modes."
        },
        "event_log": {
          "type": "string",
          "default": ".meta-viz/events.jsonl",
          "description": "Path (relative to project root) to the JSONL event log shared by all viz components."
        },
        "server": {
          "type": "object",
          "description": "Viz dashboard HTTP server configuration.",
          "properties": {
            "port": {
              "type": "integer",
              "minimum": 1024,
              "maximum": 65535,
              "default": 8765,
              "description": "TCP port the Viz dashboard (viz-report.py --serve) binds to."
            },
            "timeout_sec": {
              "type": "integer",
              "minimum": 1,
              "default": 300,
              "description": "Idle timeout (seconds) for the Viz dashboard server."
            }
          },
          "additionalProperties": false
        },
        "mcp": {
          "type": "object",
          "description": "MCP SSE server configuration.",
          "properties": {
            "port": {
              "type": "integer",
              "minimum": 1024,
              "maximum": 65535,
              "default": 9090,
              "description": "TCP port the MCP SSE server (viz-logger.py --http) binds to."
            }
          },
          "additionalProperties": false
        },
        "report": {
          "type": "object",
          "description": "Reporting policy for the Viz dashboard.",
          "properties": {
            "retention_days": {
              "type": "integer",
              "minimum": 1,
              "default": 7,
              "description": "Number of days session reports are retained on disk."
            },
            "session_timeout_min": {
              "type": "integer",
              "minimum": 1,
              "default": 5,
              "description": "Idle time (minutes) after which a session is considered closed."
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "delegation": {
      "type": "object",
      "description": "Controls agent-to-agent delegation behavior, including the A2A handoff envelope.",
      "properties": {
        "a2a_envelope": {
          "type": "boolean",
          "default": false,
          "description": "When true, agents wrap all inter-agent handoffs in the structured A2A envelope (schemas/a2a-handoff.schema.json). Enables retry tracking, timeout enforcement, and escalation policies."
        }
      },
      "additionalProperties": false
    },
    "orchestrator": {
      "type": "object",
      "description": "Orchestrator behavior configuration.",
      "properties": {
        "mode": {
          "type": "string",
          "description": "Orchestrator operating mode. Takes precedence over legacy enabled/strict booleans. 'strict' = orchestrator mandatory, no user override. 'advisory' = orchestrator recommended, user-override allowed. 'main-chat' = no orchestrator subagent, main chat acts as router + worker. Default: 'strict' (matches the legacy enabled=true/strict=true fallback derivation in sync.py when mode is unset).",
          "enum": [
            "strict",
            "advisory",
            "main-chat"
          ],
          "default": "strict"
        },
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable orchestrator mode. When false, all tasks are handled in main chat."
        },
        "strict": {
          "type": "boolean",
          "default": true,
          "description": "Strict mode: no fallback to main chat, all unknown intents are escalated."
        },
        "checkpointing": {
          "type": "boolean",
          "default": false,
          "description": "Enable persistent session checkpoints in .meta-viz/checkpoint-<timestamp>.json. Allows resuming long orchestrations after context reset. Default: false."
        },
        "native-extensions": {
          "type": "object",
          "description": "Exempt platform-native extension mechanisms (Skills, Plugins, Lifecycle-Hooks) from the STRICT-mode orchestrator gate. When enabled, platform-triggered flows are not treated as a delegation bypass. Default: true.",
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": true,
              "description": "Allow native extension mechanisms to run without going through the orchestrator gate. Branch-guard, commit-conventions and DoD still apply to resulting code changes. Default: true."
            },
            "whitelist": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "default": [],
              "uniqueItems": true,
              "description": "Whitelist of allowed native Skill/Plugin identifiers. Ist die Whitelist nicht leer, sind ausschließlich die dort gelisteten Skills/Plugins erlaubt — alles andere wird automatisch gesperrt, unabhängig vom generellen Erlaubt-Statement. Empty (default) = no filter."
            }
          },
          "additionalProperties": false
        },
        "direct-dispatch-enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable direct dispatch section in orchestrator (skip orchestrator for trivial tasks)."
        },
        "unknown-fallback": {
          "description": "Fallback behavior for unknown intents. Object form (recommended) or legacy string.",
          "oneOf": [
            {
              "type": "object",
              "properties": {
                "meta-feedback": {
                  "type": "boolean"
                },
                "main-chat": {
                  "type": "boolean"
                },
                "ask-user": {
                  "type": "boolean"
                }
              },
              "additionalProperties": false
            },
            {
              "type": "string",
              "enum": [
                "meta-feedback",
                "main-chat",
                "ask-user"
              ]
            }
          ]
        },
        "handoff": {
          "type": "object",
          "description": "A2A handoff protocol configuration.",
          "properties": {
            "protocol": {
              "type": "string",
              "description": "Handoff protocol identifier. Use 'a2a-v1' (default) or 'none'/'false' to disable.",
              "default": "a2a-v1"
            }
          },
          "additionalProperties": false
        },
        "delegation": {
          "type": "object",
          "description": "Delegation behavior configuration. Controls hard limits for agent-to-agent handoffs.",
          "properties": {
            "max_depth": {
              "type": "integer",
              "minimum": 1,
              "maximum": 50,
              "default": 10,
              "description": "Maximum delegation depth before HARD REJECT. Default 10. Self-handoff (source_agent == target_agent) is enforced separately and cannot be disabled."
            }
          },
          "additionalProperties": false
        },
        "provider-overrides": {
          "type": "object",
          "description": "Override orchestrator.mode per provider. Keys are provider names (Claude, Gemini, Opencode, Continue, Copilot, Mammouth). Providers without an entry (or without 'mode' set) inherit the global orchestrator.mode default.",
          "additionalProperties": {
            "type": "object",
            "description": "Per-provider orchestrator overrides.",
            "properties": {
              "mode": {
                "type": "string",
                "description": "Orchestrator operating mode for this provider only. Same semantics as orchestrator.mode. When unset, inherits the global orchestrator.mode.",
                "enum": [
                  "strict",
                  "advisory",
                  "main-chat"
                ]
              }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": true
    },
    "analysis": {
      "type": "object",
      "description": "AST-based file affinity analysis — opt-in parallelization hints for the Orchestrator.",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable AST-based import dependency scanning. When true, FILE_AFFINITY_HINT is populated in the orchestrator with a dependency table. Default: false."
        },
        "ast": {
          "type": "boolean",
          "default": false,
          "description": "Enable AST dependency analysis during sync. Activates analysis.py scanning and prints a summary after sync completes. Default: false."
        }
      },
      "additionalProperties": false
    }
  },
  "required": [
    "project"
  ],
  "additionalProperties": true,
  "$defs": {
    "stageItem": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "agent": {
          "type": "string"
        },
        "task": {
          "type": "string"
        },
        "mode": {
          "enum": [
            "sequential",
            "parallel_group",
            "fanout",
            "loop",
            "conditional"
          ]
        },
        "loop": {
          "type": "object",
          "properties": {
            "generator": {
              "type": "string"
            },
            "critic": {
              "type": "string"
            },
            "max_iterations": {
              "type": "integer",
              "minimum": 1
            }
          }
        },
        "parallel_group": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "agent": {
                "type": "string"
              },
              "task": {
                "type": "string"
              }
            }
          }
        },
        "condition": {
          "type": "object",
          "properties": {
            "type": {
              "type": "string"
            },
            "agent": {
              "type": "string"
            }
          }
        }
      },
      "required": [
        "id",
        "agent",
        "task",
        "mode"
      ]
    },
    "pipelineDefinition": {
      "type": "object",
      "properties": {
        "description": {
          "type": "string"
        },
        "stages": {
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/stageItem"
              }
            },
            {
              "type": "object",
              "description": "Override form keyed by stage id"
            }
          ]
        },
        "on_error": {
          "type": "string"
        }
      },
      "additionalProperties": true
    },
    "lifecycleTriggerList": {
      "type": "array",
      "description": "List of agent tasks to run for this lifecycle event.",
      "items": {
        "type": "object",
        "properties": {
          "agent": {
            "type": "string",
            "description": "Agent role to delegate the task to (e.g. 'documenter', 'validator')."
          },
          "task": {
            "type": "string",
            "description": "Task description passed to the agent."
          }
        },
        "required": [
          "agent",
          "task"
        ],
        "additionalProperties": false
      }
    },
    "outcome-caching": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false
        },
        "ttl_seconds": {
          "type": "integer",
          "minimum": 60,
          "default": 3600
        },
        "max_entries": {
          "type": "integer",
          "minimum": 10,
          "default": 100
        },
        "invalidation": {
          "type": "string",
          "enum": [
            "git-commit",
            "ttl",
            "manual"
          ],
          "default": "git-commit"
        }
      },
      "additionalProperties": false
    },
    "cascadeDefinition": {
      "type": "object",
      "description": "Definition einer SE-Kaskade mit Phasen und Agenten.",
      "required": [
        "stages"
      ],
      "properties": {
        "description": {
          "type": "string"
        },
        "enabled": {
          "type": "boolean",
          "default": true
        },
        "stages": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/cascadeStage"
          }
        }
      },
      "additionalProperties": false
    },
    "cascadeStage": {
      "type": "object",
      "description": "Eine Phase innerhalb einer SE-Kaskade.",
      "required": [
        "name",
        "agent"
      ],
      "properties": {
        "name": {
          "type": "string"
        },
        "agent": {
          "type": "string"
        },
        "parallel": {
          "type": "boolean",
          "default": false
        },
        "barrier": {
          "type": "boolean",
          "default": true
        },
        "condition": {
          "type": "string",
          "description": "Optionale Bedingung (Jinja2-ähnlich) für bedingte Ausführung."
        }
      },
      "additionalProperties": false
    }
  }
}
