# P1 Verification Results — Plan "Codex / ZCode / Kimi Code" (2026-09-04)

| | |
|---|---|
| **Date** | 2026-09-04 |
| **Plan** | `docs/plans/2026-09-04-provider-codex-zcode.md` (§11, Zeilen ~852-877, Items V1–V18) |
| **Status** | P1 COMPLETE — 18/18 Items verifiziert (davon 5 teilverifiziert/mit Widerlegung, 2 unklar) |
| **Method** | 3 parallele Worker-Batches (Codex / ZCode / Kimi Code) gegen offizielle Dokus — read-only, KEINE Code-/YAML-Änderungen, NICHT committed (git läuft separat) |
| **Legende** | 🎯 = POSTURE-IMPACT (V5, V10, V13, V15, V17, V18) · Urteile: VERIFIZIERT / TEIL / WIDERLEGT / UNKLAR |

---

## 1. Master-Tabelle (V1–V18)

| V-ID | 🎯 | Provider | Frage | Ergebnis | Quelle | Konsequenz für Plan |
|---|---|---|---|---|---|---|
| V1 | | Codex | Hook payload contract (stdin, Blocking, hooks.json vs. inline TOML) | VERIFIZIERT — stdin = 1 JSON (session_id, transcript_path, cwd, hook_event_name + Codex-Extensions `model`, `turn_id`; PreToolUse/PostToolUse/PermissionRequest + tool_name/tool_use_id/tool_input); Blocking sync-by-default; PreToolUse-Deny via `hookSpecificOutput.permissionDecision:"deny"` (+legacy exit 2 + stderr); hooks.json + inline `[hooks]` beide (alle Orte laufen, keine Ersetzung); `features.hooks` default true; Anchor v0.150.0 korrekt (rust-v0.150.0, 2026-08-26, `Interrupt` = neuestes Event) | developers.openai.com/codex/hooks · /codex/config-file/config-advanced · github.com/openai/codex/releases/tag/rust-v0.150.0 · codex-rs/hooks/schema/generated | `hook_protocol`-Entscheid kann fallen; ABER nicht 1:1 Claude: `continue`/`stopReason`/`suppressOutput` für PreToolUse/PermissionRequest nicht unterstützt (parse → failed-markiert); Trust-Review: non-managed Hooks werden per Hash-Trust ÜBERSPRUNEN bis `/hooks`-Review (Escape `--dangerously-bypass-hook-trust`) → eigener Protokoll-Wert (z.B. `codex-hooks-json`) oder claude-code-json + dokumentierte Deviations; Trust-Review-Step neu im Plan |
| V2 | | ZCode | Agents/skills/hooks/MCP/commands-Surface existiert? | VERIFIZIERT — alle 5 dokumentiert: Subagents (beta, user-level `~/.zcode/agents/<name>.md`, frontmatter name/description required, `model` inherit/ID, `thoughtLevel` — NICHT reasoningEffort, unbekannte Keys still ignoriert, `injectAgentsMd` default on seit v3.7.1); Skills (`~/.zcode/skills/<skill>/SKILL.md` + workspace-level, cap 1024 Zeichen); Hooks (7 Events, stdin = 1 Zeile JSON camelCase+snake_case Claude-kompatibel — ABER project-level Hooks werden KOMPLETT ignoriert, Log `config_project_hooks_ignored`); MCP (user `~/.zcode/cli/config.json` + workspace `.zcode/config.json`, Key `mcp.servers`, user gewinnt, kein Merge); Commands (`~/.zcode/commands/*.md` + workspace). Plugin bündelt alle 5 (seit 3.11.2 per Workspace installierbar) | zcode.z.ai/en/docs/subagents · /skill · /hooks · /mcp-services · /commands · /plugin · /qa | Capability-Wachstum verifiziert: skills/mcp/commands → on (projekt-fähig via Workspace-Scope); **`has_hooks` bleibt false für Projekt-Wiring** (Projekt-Hooks ignoriert — Hooks nur via Plugin oder User-Level); `mcp-config` = `mcp.servers` in `.zcode/config.json` |
| V3 | | Codex | AGENTS.md subdir-override/nearest-wins vs. Managed Blocks | VERIFIZIERT — Nearest-wins durch SPÄTERE Position (Konkatenation Root→cwd, "closer ... appear later"), max. 1 Datei pro Verzeichnis (AGENTS.override.md > AGENTS.md > fallback), leere übersprungen; Codex parst keine Managed-Block-Marker → Marker nicht korruptierbar; Risiken: (1) `project_doc_max_bytes` = 32 KiB KOMBINIERT über Kette (still trunciert), (2) Subdir-Dateien erscheinen NACH Managed Block (inhaltliche Konterkarierung möglich), (3) untrusted Projekte laden Root-AGENTS.md gar nicht (0.150.0) | developers.openai.com/codex/agent-configuration/agents-md · /codex/config-file/config-advanced | Shared template bestätigt (kein dedicated CODEX-Variant nötig, "dedicated only on audited conflict" bleibt Regel); Doku-Sektion: 32-KiB-Cap + Nearest-wins-Note + untrusted-Projekt-Hinweis verankern |
| V4 | | Codex | Exakter Bash-Toolname | VERIFIZIERT — `Bash` (kanonischer Hook-/Matcher-Name; shell + unified exec `exec_command` matchen beide als Bash); `apply_patch` = Edit-Tool (Matcher-Aliase `Edit`/`Write`); interne Namen `shell`/`exec_command` NICHT whitelisten; Bonus: `spawn_agent` matcht auch unter Alias `Agent` | developers.openai.com/codex/hooks (tool coverage) · /codex/config-file/config-reference | `bash_tool_name: Bash`; Whitelist um `Bash` + `apply_patch` erweitern; P1-Registry-Platzhalter ablösbar |
| V5 | 🎯 | ZCode | Exakte Toolnamen für Whitelist | VERIFIZIERT — Built-ins: `Read`/`Grep`/`Glob`/`Bash`/`Edit`/`Write`/`WebFetch`/`WebSearch`/`TodoWrite` + Dispatch-Tool `Agent`; MCP: `mcp__<server>__<tool>`; Custom-Listen exhaustiv (MCP-Tools fallen komplett weg außer voller Name) | zcode.z.ai/en/docs/subagents (#tools-and-mcp) | `provider-tools.yaml`-Whitelist = 9 + `Agent`; `bash_tool_name: Bash`, `terminal_tool` → `Bash`; P6-Platzhalter entfällt |
| V6 | | ZCode | `zcode.json` vs `.zcode/config.json` — Präzedenz | TEIL/UNKLAR — beide Orte existieren (Hooks-Doku nennt beide); nur `.zcode/config.json` als Workspace-Config mit Schema ausgewiesen (MCP: `mcp.servers`); Panel-Writes immer in `.zcode`-Native-Config; explizite Präzedenz-Regel nirgends dokumentiert | zcode.z.ai/en/docs/mcp-services · /hooks · /qa | `settings_file: .zcode/config.json` bestätigt (kanonische, schema-dokumentierte Datei); "alt: zcode.json" aus Sketch entfallen; Restklärung = P6-nice-to-have, nicht blockierend |
| V7 | | Codex | `.codex/agents/*.toml` auto-load + `multi_agent`-Enablement | VERIFIZIERT + 2 WIDERLEGUNG — Auto-load ✓ (personal `~/.codex/agents/` + project-scoped `.codex/agents/*.toml` werden als config layers geladen, kein Registrierungs-Schritt; Konflikt mit Built-ins → Custom gewinnt); Enablement ✓ kein Flag nötig (`features.multi_agent` = stable ON-by-default, `agents.enabled` default true) → Plan §4.8 `mechanism: file-based, action: none` korrekt. WIDERLEGT (1): Pflichtfeld heißt **`developer_instructions`**, nicht `instructions` (Plan §4.1/§4.5 korrigieren — Markdown-Body → `developer_instructions`). WIDERLEGT (2)/Bonus: nativer Dispatch-Tool-Surface DOKUMENTIERT: `spawn_agent`/`send_input`/`resume_agent`/`wait_agent`/`close_agent` (Plan `native_agent_tools: []` veraltet) | developers.openai.com/codex/agent-configuration/subagents · /codex/config-file/config-reference | Transform-Design (§4.5) auf `developer_instructions` korrigieren + golden files; `capabilities.native_agent_tools` von `[]` auf 5 Tool-Namen upgradierbar (verifizierter Upgrade-Pfad); `parallel_execution: true` ohne Vorbehalt |
| V8 | | Codex | MCP merge semantics + secrets-file strategy | TEIL + 1 WIDERLEGUNG — Verifiziert: Projekt-Config `.codex/config.toml` existiert (trusted projects only), `mcp_servers` NICHT in Projekt-Ignore-Liste, Key-Level-Präzedenz: CLI-Flags > Projekt (Root→cwd, closest wins) > Profile > User > System > Defaults; relative Pfade → relativ zum `.codex/`-Dir. UNKLAR: exakte Cross-Layer-Merge-Semantik für `mcp_servers` (nur Key-Level-Präzedenz dokumentiert) → P6 real-repo-test (plan-konform). WIDERLEGT: `.codex/mcp.local.toml` als secrets-file hat KEINE dokumentierte Grundlage (kein include/import-Mechanismus); dokumentierte Strategie: Env-Indirektion (`bearer_token_env_var`, `env_key`, `[mcp_servers.<id>.env]`, `http_headers_helper`) | developers.openai.com/codex/extend/mcp · /config-file/config-basic · /config-advanced · /config-reference | `secrets-file: .codex/mcp.local.toml` STREICHEN; Format-Branch `codex-toml-mcp` auf Env-Var-Indirektion im committed `.codex/config.toml` umstellen (konsistent mit secrets-template-Muster anderer Provider); Merge-Teilfrage bleibt P6 real-repo-test |
| V9 | 🎯 | ZCode | `model.lite` exakte Modell-IDs + Per-Rolle-Auswahl | TEIL-WIDERLEGT (Keys)/VERIFIZIERT (IDs+Per-Rolle) — Keys WIDERLEGT: `model.main`/`model.lite` nirgends dokumentiert; Modell-Konfig lebt in `~/.zcode/v2/config.json` (Provider-Setup, UI-verwaltet); `~/.zcode/cli/config.json` = MCP/Permission/Plugin/Hooks. IDs VERIFIZIERT: `glm-5.3` + `glm-5.3-flash` (beide lowercase, eingebaut; Flash out-of-the-box für Subscription). Per-Rolle VERIFIZIERT: `model`-Feld in Subagent-Files (ID oder `inherit`); ohne Override → Haupt-Modell geerbt | zcode.z.ai/en/docs/qa (#subagent-model) · /configuration · docs.z.ai/guides/llm/glm-5.3.md · /vlm/glm-5.3-flash.md | Tier-Mapping tragfähig (nano/fast → `glm-5.3-flash`, balanced/powerful/max → `glm-5.3`); ABER `settings_template` mit `model.main`/`model.lite` INVALIDIERT; `model: skip` im agent-transform REVIDIERBAR — Per-Rolle läuft direkt in generierten Agent-Files (ZCode unterstützt `model`-Frontmatter gerade deswegen) |
| V10 | 🎯 | Kimi | Custom-Agent-File-Format (Markdown+frontmatter vs. YAML-spec) | VERIFIZIERT → OPTION A DETERMINIERT — "plain Markdown with a frontmatter block"; Felder: `name` (optional, kebab-case, Default = Dateiname), `description` (REQUIRED), `whenToUse`, `override`, `tools` (YAML-Liste ODER komma-separierter String; MCP-Globs `mcp__github__*`), `disallowedTools`, `subagents` (flache Allowlist — KEIN kimi-cli `subagents`-Map, kein `version: 1`, kein `agent.extend` anywhere); Body = System-Prompt mit `${var}`-Platzhaltern (`${base_prompt}`, `${plugin_sections}`, `${skills}`, `${agents_md}`); Unknown fields ignoriert (Claude `model`/OpenCode `mode` explizit); fehlender `name` → Dateinamen-Fallback. Option B (YAML-spec) WIDERLEGT. Auto-discovery: Explicit > Project (`.kimi-code/agents/`, `.agents/agents/`) > Extra (`extra_agent_dirs`) > User (`~/.kimi-code/agents/`) > Plugin > Built-in | moonshotai.github.io/kimi-code/en/customization/agents (+ github.com/MoonshotAI/kimi-code) | **Option A fixieren** (Decision Rule 6.4 ausgeführt): kein neuer `frontmatter-mechanismus`-Wert, kein Serializer-Arbeit, Default-Transform-Pfad bleibt (`model: inject` + strip-fields/strip-claude-lines); Flip: `file_based_agents: true` + `agents`-Capability ON; KORREKTUR: `agents_dir: .kimi/agents` → `.kimi-code/agents/` (systematisch); `agent_ext: .md` korrekt; generierte Frontmatters laden direkt (`version` = unknown field → ignoriert); ⚠️ `.agents/agents/` = Cross-Tool-Konvention (mehrere Tools scannen dort) — sync.py muss gezielt `.kimi-code/agents/` schreiben (Cross-Provider-Kollision vermeiden) |
| V11 | | Kimi | Hooks event names + payload contract (beta) | VERIFIZIERT — ausführlich dokumentiert, nicht mehr beta-sparse; Konfig: `[[hooks]]`-Array in `~/.kimi-code/config.toml` (TOML; KEIN hooks-Dir-Mechanismus — `command` referenziert Skripte per Pfad); Felder: `event`, `matcher` (Regex), `command`, `timeout` (1-600s, Default 30); extra fields → Config-Load-Fail. Payload: JSON stdin snake_case (`{hook_event_name, session_id, session_title, client_type:"kimi_code_cli", cwd}` + event-spezifisch). Exit: 0=allow, 2=block (stderr=Grund), sonst fail-open; JSON-stdout: `hookSpecificOutput.permissionDecision`. **20 Events** (blockable nur PreToolUse/Stop/UserPromptSubmit), weitgehend Claude-kompatibel | moonshotai.github.io/kimi-code/en/customization/hooks | Hook-Contract real und Claude-nah → `hook_protocol` kann auf echter Basis fallen; #630-Warnskip bleibt sinnvoll (nur user-level config.toml dokumentiert — project-level Hook-Generierung unbestätigt, eh außerhalb sync.py-Scope). WIDERLEGT: `hooks_dir: .kimi/hooks`-Reservation GEGENSTANDSLOS (kein hooks-Dir-Autoload; Pfad wäre zudem `.kimi-code/`) |
| V12 | | Kimi | Skills/plugins directory layout | VERIFIZIERT — Skills = Markdown+frontmatter; Directory-Form `<name>/SKILL.md` (empfohlen, Scripts daneben) oder flat `<name>.md` (Directory gewinnt); Frontmatter: `name`/`description` required (Directory-Form), `type` (prompt/inline/flow), `whenToUse` (kebab/snake akzeptiert), `disableModelInvocation`, `arguments`; Platzhalter `$ARGUMENTS`, `$0`/`$1`, `${KIMI_SKILL_DIR}`; Locations: Project `.kimi-code/skills/` + `.agents/skills/`, User `~/.kimi-code/skills/` + `~/.agents/skills/`; `merge_all_available_skills` default true | moonshotai.github.io/kimi-code/en/customization/skills (+ /plugins) | `skills_dir: .kimi/skills` → `.kimi-code/skills/` umschreiben (Reservation → echter Pfad); skills-Capability-Wachstum real aktivierbar |
| V13 | 🎯 | Kimi | MCP config location/format + secrets split | VERIFIZIERT — MCP-Konfig = **`mcp.json` (JSON)**: User `~/.kimi-code/mcp.json` (`$KIMI_CODE_HOME/mcp.json`), Project `.kimi-code/mcp.json`; project overridden user bei Namensgleichheit; Format `{"mcpServers": {...}}` — stdio (command/args/env/cwd), HTTP (url), SSE (`transport:"sse"`); optional `headers`, `bearerTokenEnvVar`, `enabled`, `startupTimeoutMs`, `toolTimeoutMs`, `enabledTools`/`disabledTools`. KEIN separates Secrets-File — Env-Indirektion via `bearerTokenEnvVar` + `env`-Map; API-Keys in config.toml `[providers.*]` (user-level); Trust-Prompt für project-level MCP in unvertrauten Foldern | moonshotai.github.io/kimi-code/en/customization/mcp (+ /configuration/config-files) | **6.7-Entscheid → Option 3 verifiziert (wire-reuse):** `mcpServers`-Top-Level-Key in JSON wire-identisch zum bestehenden `claude-settings`/`gemini-settings`-Branch (`_update_json_config(path, "mcpServers", ...)`); KEIN neuer `kimi-toml-mcp`-Branch (Option 2 entfällt); `mcp-config: {}` → format-reuse + `committed-file: .kimi-code/mcp.json` (Pfadkorrektur); secrets-split folgt claude-settings-Muster, Kimi-nativ zusätzlich `bearerTokenEnvVar` |
| V14 | | Kimi | Bash/Tool names für provider-tools.yaml | VERIFIZIERT — voller Built-in-Katalog: `Read`/`Write`/`Edit`/`Grep`/`Glob`/`ReadMediaFile`/`Bash`/`WebSearch`/`FetchURL`/`EnterPlanMode`/`ExitPlanMode`/`TodoList`/`Agent`/`AgentSwarm`/`AskUserQuestion`/`Skill`/`TaskList`/`TaskOutput`/`TaskStop`/`WaitFor`/`CronCreate`/`CronList`/`CronDelete`; MCP: `mcp__<server>__<tool>`. `bash_tool_name` = **`Bash`** (exakt, case-sensitive, 4 Belege: hooks-Matcher, permission-Pattern, `[background]`-Doku, tools-Referenz); KEIN `terminal_tool`-Mapping nötig (direkte 1:1-Namen) | moonshotai.github.io/kimi-code/en/reference/tools | P1-Registry-Platzhalter ablösbar: `bash_tool_name: Bash`; Whitelist aus Katalog befüllbar; ⚠️ Case-Sensitivity: falsch geschriebene Namen matchen nie + Warning — golden tests exakte Schreibweise absichern |
| V15 | 🎯 | Kimi | Project-level config file name/path | TEIL-WIDERLEGT/UNKLAR — User: `~/.kimi-code/config.toml` (+ companion `tui.toml`). Project: **`.kimi-code/local.toml`** existiert, ABER dokumentierter Inhalt nur `[workspace] additional_dir`; ausdrücklich maschinenspezifisch, "typically should not be shared", `.gitignore`-Empfehlung, `/add-dir` auto-erstellt; KEIN project-level `config.toml` dokumentiert. Nebenfund: Doku-Precedence = env vars/startup > config.toml (z.B. "per-server field > environment variable > config.toml > built-in default"); 6.3-Note "project > global > startup (highest)" so nicht haltbar (Projekt>Global nur für mcp.json belegt); echtes Policy-Surface existiert: `[[permission.rules]]` (allow/deny/ask, Patterns) + `dangerous_command_guard` + `[tools] enabled/disabled` | moonshotai.github.io/kimi-code/en/configuration/config-files | Plan-Annahme `settings_file: .kimi/config.toml` doppelt falsch: Pfad → `.kimi-code/`, Name → `local.toml`; ABER local.toml = kein generelles Settings-File → **Plan-Default "defer" bleibt**, `has_settings`-Capability erst bei echtem Bedarf; 6.3-Precedence-Note KORRIGIEREN (env > config); Isolation-Skip-Path kann später revisited werden (Policy-Surface verifiziert) |
| V16 | | Kimi | Subagent dispatch tool surface / bootstrap | VERIFIZIERT — 2 Dispatch-Tools: **`Agent`** (auto-allow; Parameter `prompt`, `description` (3-5 Wörter), `subagent_type` (Default `coder`), `resume` (mutually exclusive mit subagent_type), `run_in_background`, `model` bei Pool) + **`AgentSwarm`** (fan-out: `prompt_template` + `items`-Array, bis 128, aggregated report, einziger Toolcall pro Response). Bootstrap: NICHTS nötig ("discovers custom agents automatically alongside the built-in ones"); Permission-Inheritance vom Main Agent; Depth nativ geregelt: 3 Built-ins (coder/explore/plan) dispatchen nicht weiter, Custom erben Allowlist, tiefere Chains nur per explizitem `subagents`-Frontmatter-Opt-in; hooks: SubagentStart/SubagentStop | moonshotai.github.io/kimi-code/en/customization/agents (+ /reference/tools, /configuration/config-files) | `native_agent_tools: []` → **`["Agent", "AgentSwarm"]`**; `file_based_agents: true` (mit V10); delegation-syntax.yaml-Entry implementierbar (`Agent` single / `AgentSwarm` fan-out, `subagent_type` = Rollenname); Roster-Bootstrap entfällt weitgehend (auto-discovery ersetzt registration; Orchestrator-Hinweis genügt); `parallel_execution: false` hochstufbar (AgentSwarm = paralleler fan-out mit dokumentiertem Contract) |
| V17 | 🎯 | ZCode | Exakter nativer Agent-Dispatch-Toolname | VERIFIZIERT — Toolname **`Agent`** (Legacy-Alias **`Task`**); 2 Belege: Hooks-Matcher-Regel ("Tool events match the actual tool name, with `Agent`/`Task` aliases supported") + Subagents-Seite ("launches a subagent through the Agent tool"); Hook-Payload SessionStart führt optional `agent_type` | zcode.z.ai/en/docs/hooks (#executors) · /subagents | `native_agent_tools: [Agent]` (+ `Task`-Alias-Notiz); delegation-syntax.yaml-ZCode-Entry kann konkreten Namen bekommen — §5.8 Dispatch-Layer implementierbar |
| V18 | 🎯 | ZCode | Inline-Persona pro Agent-Call? | UNKLAR → Befund spricht für "nur Name/Description + Task-Prompt" — KEIN dokumentierter Agent-Tool-Parameter für Inline-System-Prompt pro Call; Persona lebt ausschließlich in Definitions-Datei (Body = System-Prompt), Auswahl über registrierte Identität; Task-Prompt = Freitext (Orchestrator kann Persona + A2A-Envelope als task text an Built-in `general-purpose` übergeben = §5.8 Layer 3). WICHTIGSTE KORREKTUR: ZCode hat DOCH ein file-based Agent-Inventory — `~/.zcode/agents/*.md` wird von Runtime geladen → §5.2 `file_based_agents: false` ("verified — nothing auto-loads") INVALIDIERT (user-level); Workspace-scope-Verteilung verifiziert über PLUGIN (`agents/*.md`, per-Workspace seit 3.11.2) | zcode.z.ai/en/docs/subagents (#custom-subagents, #scope) · /plugin (#develop) · /qa (#subagent-model) | §5.8-Fallback (compact roster, read on demand) als PRIMÄRPFAD; Bootstrap-Injection (Layer 2) bleibt korrekt, ABER Definitionen dahin, wo Runtime lädt: `~/.zcode/agents/` (user-level, verifiziert auto-load) oder Plugin-Route (workspace-scoped); §5.2 `file_based_agents` RE-VERIFIZIEREN/REVIDIEREN; `<workspace>/.zcode/agents/`-auto-load = offener P6 real-repo-test |

---

## 2. POSTURE-IMPACT-Kurzfassung (je V)

| V-ID | Provider | POSTURE-Result |
|---|---|---|
| V5 🎯 | ZCode | VERIFIZIERT — kein invalidierender Impact: Whitelist (9 + `Agent`), `bash_tool_name: Bash` direkt umsetzbar; P6-Platzhalter entfällt |
| V10 🎯 | Kimi | VERIFIZIERT → **Option A determiniert** (Markdown+YAML-frontmatter bestätigt, Option B widerlegt): kein neuer mechanism-Wert, kein Serializer; Flip `file_based_agents: true` + `agents`-Capability ON; `.kimi/` → `.kimi-code/` systematisch |
| V13 🎯 | Kimi | VERIFIZIERT → **6.7 Option 3 (wire-reuse)**: `{"mcpServers": {...}}` wire-identisch zum claude-settings-Branch; Option 2 (kimi-toml-mcp) entfällt; secrets via `bearerTokenEnvVar`/`env` statt separates File |
| V15 🎯 | Kimi | TEIL-WIDERLEGT → Plan-Default "defer" bleibt: `settings_file: .kimi/config.toml` falsch (Pfad `.kimi-code/`, Name `local.toml`, Inhalt nur `[workspace] additional_dir`, nicht shared); `has_settings` erst bei Bedarf; 6.3-Precedence-Note korrigieren (env > config); Policy-Surface verifiziert (Isolation-Revisit möglich) |
| V17 🎯 | ZCode | VERIFIZIERT — `Agent` (+ `Task`-Alias): delegation-syntax-ZCode-Entry + §5.8-Dispatch-Layer implementierbar |
| V18 🎯 | ZCode | UNKLAR → Primärpfad = "nur Name/Description + Task-Prompt" (kein Inline-System-Prompt); §5.8 Layer-2 = Bootstrap-Roster + Definitionen dahin, wo Runtime lädt; **§5.2 `file_based_agents: false` invalidiert** (user-level `~/.zcode/agents/` auto-load verifiziert) |

---

## 3. Cross-cutting Korrekturen (plan-wide)

1. **Kimi: `.kimi/` → `.kimi-code/` systematisch** — 6.5-Sketch, 5-Path-collision-tests, isolation-dirs, gitignore-Entries: docs-consistent sind `.kimi-code/agents/`, `.kimi-code/skills/`, `.kimi-code/mcp.json`, `.kimi-code/local.toml`, User-Root `~/.kimi-code/` (rename via `KIMI_CODE_HOME`). ⚠️ sync.py gezielt `.kimi-code/` schreiben, NICHT `.agents/` (cross-tool-Konvention — kollidiert mit anderen Tools, die dort scannen).
2. **Codex: `developer_instructions` statt `instructions`** — Transform-Design (§4.5) + golden files korrigieren.
3. **Codex: secrets-file streichen** — Env-Var-Indirektion im committed `.codex/config.toml` (`bearer_token_env_var`/`env`), konsistent mit Muster anderer Provider.
4. **ZCode: `file_based_agents` revidieren** — §5.2 invalidiert (user-level auto-load verifiziert); Workspace-Verteilung via Plugin; `<workspace>/.zcode/agents/`-auto-load = P6-Test.
5. **ZCode: Per-Rolle direkt in Agent-Files** — §5.3 `model: skip` revidieren (ZCode unterstützt `model`-frontmatter); Tier-Mapping bleibt (`glm-5.3-flash`/`glm-5.3`).
6. **Codex/ZCode/Kimi: native dispatch surfaces alle DOKUMENTIERT** — `native_agent_tools` überall befüllbar (`spawn_agent`+4 / `Agent`+`AgentSwarm` / `Agent`+`AgentSwarm`); Upgrade-Pfade verifiziert.
7. **ZCode `has_hooks` bleibt false für Projekt-Wiring** — Projekt-Hooks werden ignoriert (`config_project_hooks_ignored`); Capability-Wachstum nur skills/mcp/commands.
8. **Kimi `hooks_dir`-Reservation gegenstandslos** — kein hooks-Dir-Autoload; #630-warnskip-Generation-Posture bleibt (nur user-level dokumentiert).

---

## 4. Offene Punkte (P6, plan-konform)

- Codex: exakte Cross-Layer-Merge-Semantik für `mcp_servers` (nur Key-Level-Präzedenz dokumentiert) — real-repo-test steht im Plan.
- ZCode: `<workspace>/.zcode/agents/`-auto-load (nur user-level dokumentiert; workspace via Plugin verifiziert) — real-repo-test.
- ZCode: `zcode.json` vs `.zcode/config.json` explizite Präzedenz — P6-nice-to-have.
- Kimi: project-level Hook-Generierung (nur user-level config.toml dokumentiert) — außerhalb sync.py-Scope.

---

## 5. DoD-Konventionen (P1)

- Verifikation read-only: KEINE Code-/YAML-Änderungen (nur diese Ergebnisdatei).
- NICHT committed — git läuft separat (Hinweis: `feat/`-branch-Policy gilt).
- Jedes Item: datiertes Ergebnis (Stand 2026-09-04, Doku-Stand: Codex rust-v0.150.0+, ZCode 3.11.2, Kimi Code aktuelle Doku 2026-09).
