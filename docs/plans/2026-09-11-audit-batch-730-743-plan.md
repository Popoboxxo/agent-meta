# Audit Batch Triage & Implementation Plan — #712, #730–#743

## STATUS

- **Date:** 2026-09-11
- **HEAD:** 56da703e
- **VERSION:** 1.1.0
- **Mode:** read-only triage — no code fixes, no commits, no generated/wiki artifacts touched
- **Batch:** #712, #730, #731, #732, #733, #734, #735, #736, #737, #738, #739, #740, #741, #742, #743 (15 issues)
  — note: **#742 is a merged PR, not an issue**
- **Recommended top-3 order:** WP1 (config correctness) → WP4 (CI/scenario contract) → WP2 (provider-agnostic commands)
- **Decisions confirmed 2026-09-11:** #712 (P1 + closed, residual fixed), #730 (P1 + body), #739 (P2), #742 (invalid — merged PR), #736 (body), pre-commit → CI-only (WP4).
- **WP0 hygiene executed 2026-09-11:** triage closure done — see WP0 outcome below; the XS `.gitattributes` residual was implemented directly (generator-first check negative).

## Scope/Method

- **Source document:** `docs/plans/audit-2026-09-detailed-system-audit.md` — dated **2026-09-03**, written against **VERSION 0.101.0-beta.4**.
- The original audit was largely remediated between 2026-09-03 and 2026-09-11; this batch is the follow-up triage. Every finding below was re-verified against current `main` @ `56da703e` / VERSION 1.1.0.
- **Method:** per-issue root-cause verification, severity re-derivation (verified severity wins over the issue label), duplicate/overlap analysis, dependency + collision clustering, work-package derivation, and a test/verification strategy per package.
- **In scope:** triage, ordering, branch/PR scoping, acceptance criteria, risk register.
- **Out of scope:** any implementation or code change, issue/PR mutation, wiki/CHANGELOG/index updates, git operations.
- **Triage-Status legend:** `valid` = reproduces on current main · `already-fixed-on-main (core)` = core defect no longer present, only residual hygiene left · `INVALID` = not an actionable issue.
- **Spot-checks performed (line numbers in the source triage may drift):** hardcoded `"agent-meta-version": "0.90.10"` currently at `scripts/lib/config.py:66` (not `:14`); admin-server writable-section allow-set at `scripts/admin-server.py:4623-4632`; schema provider enum at `config/project-config.schema.json:36-59`; `agents/1-generic/feature.md` absent (glob-confirmed); `.github/workflows/validate.yml` runs only `--validate` (no `--check`).

---

## 1. Per-Issue Triage

| # | Titel | Severity | Triage-Status | Root-Cause-Hypothese | Betroffene Dateien/Module | Größe |
|---|-------|----------|---------------|----------------------|---------------------------|-------|
| #712 | orchestrator-guard fails open (wrapper missing) | verified P1 (issue said P0) | already-fixed-on-main (core) | Hook files deployed before registration; `--validate` mitigation landed in #723 / commit `9381bcaa` via `check_hook_enablement_consistency`; sync-ordering bug not reproducible | `hooks/1-generic/orchestrator-guard.sh`, `hooks/1-generic/orchestrator-guard-impl.sh`, `.claude/settings.json`, `scripts/lib/consistency/hook_drift.py`, `scripts/lib/hooks.py`; residual: `.gitattributes` | XS |
| #730 | Admin-UI config sections, silent PUT rejection, a11y | P1 (derived, no label) | valid | Writable-section allow-set omits `hooks`, `debug-mode`, `tier-overrides`, `mcp-role-overrides`, `backup` → silent PUT rejection + data-loss on save | `scripts/admin-server.py` (~4623), `docs/ui/admin-ui.html` | L |
| #731 | `--fill-defaults` pins 0.90.10 instead of VERSION | P1 | valid | Hardcoded `"agent-meta-version": "0.90.10"` in field defaults while `VERSION` = 1.1.0 | `scripts/lib/config.py` (~66), `tests/test_version_bookkeeping.py` | S |
| #732 | ai-providers enum 6/9 + silent Claude fallback | P1 | valid | JSON-schema enum lists only 6 providers, `provider-capabilities.yaml` has 9; unknown provider silently falls back to Claude instead of erroring | `config/project-config.schema.json:36-59`, `scripts/lib/config.py`, `scripts/lib/consistency/provider_registry_completeness` | M |
| #733 | Unresolved `{{LANG}}`-placeholders in rules | P2 | valid | 8 variables (`*_LANGUAGE`, `PROJECT_GOAL`/`PROJECT_LANGUAGES`, `CODE_CONVENTIONS`) have neither a default nor a `_VARIABLE_FALLBACKS` entry | `rules/1-generic/language.md`, `rules/1-generic/commit-conventions.md`, `scripts/lib/config.py` | M |
| #734 | Drift-overwrite without backup despite doc promise | P2 | valid | `generated_file_drift.py` contains no backup code (warn-only by design); only `context.py:174` writes `.sync-backup-*` | `scripts/lib/generated_file_drift.py`, `scripts/lib/context.py:174`, `knowledge/wiki/*` (8×), `snippets/sync-concept.md` | S |
| #735 | 22× `if provider ==`, 5/9 providers without commands | P1 | valid | Provider-specific branching by name, else-branch returns silently; no `commands` capability in `provider-capabilities.yaml`. Violates provider-agnostic policy | `scripts/lib/commands.py:124-143` (dispatch 191-212), `config/provider-capabilities.yaml`, `scripts/lib/{roles,rules,sync_pipeline,viz,context,providers,agent_sync}.py` | L |
| #736 | Role-registry drift, `feature` role, `provider-expert` orphan | P2 | valid | Example references role `feature` but `agents/1-generic/feature.md` does not exist (drift larger than reported); `provider-expert.md` exists but is unregistered; `--audit-config` is project-scoped, blind to framework drift | `config/role-defaults.yaml`, `config/project-config.schema.json`, `howto/configs/project.yaml.example:115`, `agents/1-generic/provider-expert.md` | M |
| #737 | 41 warnings, `project.short` never filled, inverted hint | P2 | valid | Warning text is inverted ("fix or install jsonschema to suppress this check"); `fill_defaults()` never populates `project.short` (only read at `:602`) | `scripts/lib/config.py:156-158` (hint at `:209` spot-checked), `fill_defaults()`, pipeline warning path | M |
| #738 | `--audit-config` writes `env.*` + `sync.log` | P3 | valid | `_handle_audit_config` sets only `ctx.mode`; `_run_common_tail` then executes the `env.py` writer, producing 4 `env.*` files + `sync.log` | `scripts/lib/cli_commands.py` (`_handle_audit_config:579`, `_run_common_tail:1113`), `scripts/lib/env.py` | S |
| #739 | Stale committed context-files, CI blind | verified P2 (issue said P1) | already-fixed-on-main (core) | `AGENTS.md:82` + `CLAUDE.md:6` now show v1.1.0, both cited tests pass; residual = no `--check` in CI, `orchestration-test.yml` paths-filter omits `VERSION`/`CHANGELOG` | `.github/workflows/validate.yml`, `.github/workflows/orchestration-test.yml`, `AGENTS.md`, `CLAUDE.md` | S |
| #740 | Scenario 21 red on main (wrong contract) | P1 | valid | Assert expects `eligible_roles == []` while `auto_commit.py:135-143` documents capability data as mode-independent; unit tests cover the newer behavior; scenario suite not run in CI | `tests/scenarios/asserts/21-auto-commit-off-ignores-config.sh:76`, `tests/scenarios/run.sh` | S |
| #741 | Raw `AttributeError` on null config blocks | P2 | valid | Explicit null blocks not normalized; `_build_core_variables` uses `config.get("project", {})` → `None.get` crashes when `project: null`. **Blocker for #733, #737** | `scripts/lib/config.py` (`load_config:106`, `_build_core_variables:182`) | S |
| #742 | "fix: resolve issue batch …" | — | INVALID (merged PR) | Not an issue; `gh pr view 742` → MERGED 2026-09-11T02:35Z, fixes #677/#675/#728/#729/#726/#318; contains commit `56da703e` which also resolved #739 core | — | — |
| #743 | `checkpointing: true` is a no-op | P2 | valid | `CHECKPOINTING_BLOCK` referenced by config but no template/agent consumes it; `PROGRESS_CHAT_PUSH_ENABLED=false` hinges on `all_providers_support_hooks()` so Gemini/Opencode lack `hook_protocol` | `agents/1-generic/orchestrator.md` (§9 unconditional), `snippets/orchestrator/checkpointing.md`, `scripts/lib/config.py:840/1165`, `scripts/lib/providers.py:233` | M |

### Per-Issue Notes

- **#712:** Core defect is gone on main. The requested `--validate` mitigation already shipped (#723 / `9381bcaa`). The sync-ordering bug is not reproducible. The only residual is a missing `.gitattributes` entry for the hook scripts — close the P0/P1 alarm, file (or fold) the XS residual.
- **#730 (body inaccuracy):** `permission-mode-overrides` **is already** in the allowed set (spot-checked at `admin-server.py:4624`). The genuinely missing keys are `hooks`, `debug-mode`, `tier-overrides`, `mcp-role-overrides`, `backup`. Scope the fix to silent-rejection → explicit error + the actual missing keys, not to `permission-mode-overrides`.
- **#731:** `tests/test_version_bookkeeping.py` already exists — extend it to assert the default equals `VERSION` rather than hardcoding a literal.
- **#732 ↔ #736 (overlap):** both are instances of the same anti-pattern — a hand-maintained enum drifting from the generated/registry source. #736 shares the enum-regeneration path with #732.
- **#739:** Core already fixed by `56da703e` (from merged PR #742). Residual is purely CI hardening.
- **#740:** `auto_commit.py` documents capability data as mode-independent; the scenario asserts the obsolete contract. Fix the assert, then wire the scenario suite into CI.
- **#741:** Must land **first** in the CONFIG cluster — it blocks #733 and #737.
- **#742:** Purely housekeeping — close as invalid; do not create a work package for it.
- **#743:** `CHECKPOINTING_BLOCK` is referenced in `scripts/lib/config.py:1165` and `scripts/lib/consistency/placeholders.py`, but `agents/1-generic/orchestrator.md` §9 hardcodes the checkpoint instruction unconditionally and no template consumes the block. Full provider coverage of the chat push also depends on 674 phase 3.1 (Antigravity/provider hooks).

---

## 2. Abhängigkeiten & Cluster

### Clusters

- **CONFIG (highest collision risk — all touch `scripts/lib/config.py`):** #741, #731, #732, #733, #737, #736.
  - **Blocker:** #741 → blocks #733 and #737 (robustness first, then defaults/warning logic).
  - **Shared path:** #736 shares the enum-regeneration path with #732.
- **PROVIDER (shared data source `config/provider-capabilities.yaml`):** #735, #732, #743.
  - #735 (capability data + dispatch) informs #732 (derive enum from registry) and #743 (per-provider `hook_protocol` for chat push). No hard blocker, but sequencing WP2 after WP1 #732 avoids re-touching the same registry.
- **CI/PROCESS:** #739 (residual) + #740 — both touch `.github/workflows/*`; **serialize** to avoid workflow-file conflicts.
- **GENERATED-ARTIFACT-TRUTH:** #734 + #733 + #739 — all on the "generated files vs. docs" axis.
- **HOOKS:** #712 alone (isolated, residual XS).
- **ADMIN-UI:** #730 alone (isolated, L).

### Blockers (A → B means A must land before B)

| Blocker | Blocked | Reason |
|---|---|---|
| #741 | #733, #737 | Null-block normalization must exist before defaults/fallback and warning logic are changed. |
| #732 (WP1) | #735 → #743 (WP2) | Provider enum must be derived from the registry before dispatch/`hook_protocol` work re-touches the same data. |
| #739 (CI residual) | #740 (CI wiring) | Both modify workflow files; serialize to keep PRs conflict-free. |

### Duplicates / Overlaps

- **No true duplicates** among the 15.
- Overlaps: #732 ↔ #736 (hand-maintained enum drift), #733 ↔ #737 (placeholder leak referenced separately), #739 ↔ #740 (both "main/CI red", different root causes).

### Roadmap #674 Relations

- None of the 15 issues appear in `docs/plans/2026-09-05-issue-674-roadmap.md`; they are the follow-up audit batch 2026-09/10-11.
- Indirect links: #739 ← #540 (674 phase 4a); #743 depends on 674 phase 3.1 (Antigravity/provider hooks) for full provider coverage; #712 relates to guard hardening #516/#542/#551.
- Older audit cluster #642–#654 is already CLOSED.

---

## 3. Work Packages

Ordered by verified severity P0→P3, respecting blockers. One branch + one PR per package (WP0 being issue hygiene only).

### WP0 — Triage Closure / Housekeeping — ✅ DONE (2026-09-11)

**Outcome (2026-09-11):**

- **#742** declared INVALID (already-merged PR) — PR comment posted, no issue to close; excluded from the issue burn-down.
- **#712** P0 label removed / P1 set; residual implemented as repo-root `.gitattributes` entry `hooks/1-generic/orchestrator-guard*.sh text eol=lf` (forces LF, created directly because the generator-first check was negative); issue CLOSED as completed with a resolution comment.
- **#730** P1 label set; issue body CORRECTED — `permission-mode-overrides` is already allowed (`scripts/admin-server.py:4624`); the genuinely missing writable sections are `hooks`, `debug-mode`, `tier-overrides`, `mcp-role-overrides`, `backup`.
- **#739** P1 label removed / P2 set; comment added — core fixed by #742 / commit `56da703e`, remaining scope is CI hardening only (WP4).
- **#736** issue body CORRECTED — `agents/1-generic/feature.md` does not exist at all; the drift is larger than originally reported.
- **Pre-commit gate** decided CI-ONLY — no `.pre-commit-config.yaml` is created; WP4 adds `python3 scripts/sync.py --check` to `.github/workflows/validate.yml`.

- **Issues:** #742 (invalid), #712 (residual only)
- **Rationale:** Remove false positives from the batch before any engineering. #742 is a merged PR, not an issue; #712's core is fixed on main.
- **Branch / PR:** No branch for #742 (GitHub-only close). Optional `chore/audit-wp0-gitattributes` for the XS #712 residual.
- **PR scope — in:** add `.gitattributes` entry for `hooks/1-generic/orchestrator-guard*.sh` if the residual is confirmed still missing; nothing else.
- **PR scope — out:** any guard/hook logic change; re-opening the sync-ordering investigation.
- **Dependencies:** none.
- **Acceptance criteria:**
  1. #742 is classified INVALID (already-merged PR, not an issue) — verified via `gh pr view 742`, annotated with a PR comment; there is no issue to close, so it is excluded from the issue burn-down.
  2. #712 residual either fixed by a one-line `.gitattributes` change or explicitly closed with a note; no guard logic touched.
  3. `tests/test_hook_enablement_consistency.py` + `tests/test_orchestrator_guard_hook.py` stay green.

### WP1 — Config Correctness & Robustness (P1; CONFIG cluster, runs first)

- **Issues:** #741 (first, blocker) → #731 → #732
- **Rationale:** #741 gates the P2 defaults/warning work (#733, #737); #731 and #732 are cheap P1 correctness fixes in the same module/schema, so they batch naturally.
- **Branch:** `fix/audit-wp1-config-correctness`
- **PR scope — in:** `scripts/lib/config.py` null-block normalization; version default derived from `VERSION`; provider enum derived from `config/provider-capabilities.yaml` + explicit error/reject on unknown provider instead of silent Claude fallback; extend `tests/test_version_bookkeeping.py`, `tests/test_config_audit.py`, and the provider-registry completeness check.
- **PR scope — out:** provider dispatch refactor (#735, WP2); defaults/placeholder fallbacks (#733/#737, WP5); `--audit-config` side effects (#738, WP7).
- **Dependencies:** none (WP1 is upstream).
- **Acceptance criteria:**
  1. `project: null` / other explicit-null blocks load without `AttributeError`; unit test added for each null block.
  2. `--fill-defaults` writes exactly `VERSION` (1.1.0), asserted by test (no hardcoded literal).
  3. An unknown `ai-providers` value fails schema/validation with a clear error; the enum contains all 9 registry providers; `provider_registry_completeness` is green.
  4. `python3 scripts/sync.py --validate` and `python -m pytest tests/ -q` pass.

### WP2 — Provider-Agnostic Commands (P1; PROVIDER cluster)

- **Issues:** #735 → #743
- **Rationale:** Removes `if provider ==` branching and the silent else-branch by adding a `commands` capability; then makes `checkpointing`/chat-push actually provider-driven. Depends on WP1 #732 having a complete registry.
- **Branch:** `fix/audit-wp2-provider-agnostic-commands`
- **PR scope — in:** `config/provider-capabilities.yaml` (`commands`, `hook_protocol`); capability-driven dispatch replacing literal provider-name branches in `scripts/lib/{commands,roles,rules,sync_pipeline,viz,context,providers,agent_sync}.py`; wire `CHECKPOINTING_BLOCK` into `agents/1-generic/orchestrator.md`/snippet so `checkpointing: true` is honored; provider-specific tests.
- **PR scope — out:** inventing parallelism contracts for unverified providers; full Antigravity hook work (674 phase 3.1) beyond what #743 needs.
- **Dependencies:** WP1 (#732) — requires the registry enum to be complete before deriving dispatch from it.
- **Acceptance criteria:**
  1. No new `if provider == "Name"` branch is introduced; touched call sites resolve behavior via capability flags.
  2. All 9 providers have an explicit `commands` entry (true or explicit unsupported) — no provider hits a silent fallback.
  3. `checkpointing: true` produces consumer-visible behavior; `checkpointing: false` suppresses it.
  4. `tests/test_pipelines.py`, `tests/test_routing_tool_definitions.py`, provider-specific tests, and the consistency check pass.

### WP3 — Admin-UI Writable Config (P1; isolated, L)

- **Issues:** #730
- **Rationale:** Isolated from all other packages; can proceed in parallel. Fixes silent data-loss on save plus a11y gaps.
- **Branch:** `fix/audit-wp3-admin-ui-writable-config`
- **PR scope — in:** extend `scripts/admin-server.py` allow-set with `hooks`, `debug-mode`, `tier-overrides`, `mcp-role-overrides`, `backup`; return an explicit 4xx error on non-writable section instead of silently ignoring; render the missing config sections and a11y fixes in `docs/ui/admin-ui.html`.
- **PR scope — out:** broader admin-server split (`docs/plans/admin-server-split-backlog.md`); unrelated UI redesign.
- **Dependencies:** none (parallelisable).
- **Acceptance criteria:**
  1. Saving each newly-allowed section persists and round-trips.
  2. A disallowed section yields a visible error (no silent rejection, no partial write).
  3. `tests/test_admin_server.py` and `tests/browser/` pass; manual a11y check of touched controls.

### WP4 — CI & Scenario Contract (P1; CI/PROCESS cluster, serialize)

- **Issues:** #740 + #739 (residual)
- **Rationale:** Main/CI is red or blind. Cheap S fixes that restore signal quality before larger work.
- **Branch:** `chore/audit-wp4-ci-scenario-contract`
- **PR scope — in:** correct the scenario-21 assert to the mode-independent capability contract (`tests/scenarios/asserts/21-auto-commit-off-ignores-config.sh`); add the scenario suite to CI; add `sync.py --check` to `validate.yml`; add `VERSION`/`CHANGELOG` to `orchestration-test.yml` paths-filter.
- **PR scope — out:** changing `auto_commit.py` behavior (the unit tests already encode the correct contract); unrelated workflow refactors.
- **Dependencies:** serialize #739-residual before/with #740 (same workflow files).
- **Acceptance criteria:**
  1. `tests/scenarios/run.sh` exits 0 on `main`; scenario 21 asserts the documented contract.
  2. The scenario suite runs in CI (or a follow-up issue is filed if runtime cost is prohibitive).
  3. `sync.py --check` runs in `validate.yml` and fails on drift.
  4. `orchestration-test.yml` triggers on `VERSION` and `CHANGELOG` changes.

### WP5 — Config Defaults & Warnings (P2; CONFIG cluster, after #741)

- **Issues:** #733, #737, #736
- **Rationale:** Depends on #741's null-safety. Adds missing variable defaults/fallbacks, fixes the inverted warning, populates `project.short`, and cleans role-registry drift.
- **Branch:** `fix/audit-wp5-config-defaults-warnings`
- **PR scope — in:** `_VARIABLE_FALLBACKS`/defaults for the 8 leaking variables; correct inverted jsonschema hint; populate `project.short` in `fill_defaults()`; remove/align the `feature` role reference (or add the missing `agents/1-generic/feature.md`); register or remove `provider-expert`; make `--audit-config` detect framework-level drift.
- **PR scope — out:** enum derivation (done in #732/WP1); renderer/kernel changes unrelated to these variables.
- **Dependencies:** WP1 (#741) — must land first.
- **Acceptance criteria:**
  1. `python3 scripts/sync.py --validate` emits 0 unresolved `{{…}}` placeholders for the 8 variables.
  2. Warning text is no longer inverted; `project.short` is populated by `--fill-defaults`.
  3. No example/schema reference to a non-existent role remains; `provider-expert` is either registered or removed.
  4. `tests/test_config_variable_fallbacks.py`, `tests/test_rule_variables.py`, `tests/test_config_audit.py` pass.

### WP6 — Generated-Artifact Backup (P2)

- **Issues:** #734
- **Rationale:** Resolve the doc-vs-code mismatch: either implement backup-before-overwrite in drift handling or correct the promise in docs. Isolated to the drift path.
- **Branch:** `fix/audit-wp6-generated-artifact-backup`
- **PR scope — in:** backup-on-overwrite in `scripts/lib/generated_file_drift.py` (reusing the `context.py:174` `.sync-backup-*` pattern) **or** docs correction in `snippets/sync-concept.md`; update the 8 `knowledge/wiki/*` references accordingly.
- **PR scope — out:** changing drift detection semantics/allowlist; touching `backup.py` archive feature.
- **Dependencies:** none.
- **Acceptance criteria:**
  1. A drift-triggered overwrite either writes a `.sync-backup-*` sibling or the docs no longer promise one — verified by a targeted test in `tests/test_generated_file_drift*.py`.
  2. Generator-first ordering respected: any `scripts/lib` change lands before regenerated artifacts, keeping `test_generated_file_drift*` green.
  3. All docs/wiki references match actual behavior.

### WP7 — Audit-Config Side Effects (P3)

- **Issues:** #738
- **Rationale:** Lowest severity; `--audit-config` should be read-only but currently writes `env.*` + `sync.log`.
- **Branch:** `fix/audit-wp7-audit-config-side-effects`
- **PR scope — in:** make `_handle_audit_config` short-circuit before `_run_common_tail`'s env writer (or add an explicit read-only mode flag).
- **PR scope — out:** refactoring the shared tail for other modes.
- **Dependencies:** none.
- **Acceptance criteria:**
  1. `--audit-config` writes no `env.*` files and no `sync.log`; asserted by a test.
  2. Other modes' env-writing behavior is unchanged.

---

## 4. Test- & Verifikationsstrategie

**Baseline commands (run per WP):**

```bash
python scripts/sync.py --dry-run
python scripts/sync.py
python3 scripts/sync.py --validate
python scripts/sync.py --check
python scripts/consistency-check.py --changed
python -m pytest tests/ -q
tests/scenarios/run.sh
```

> Ordering rule for every WP touching `scripts/lib`: kernel/generator first, then regenerate artifacts — otherwise `tests/test_generated_file_drift*` goes red.

| WP | Concrete tests | Extra commands |
|---|---|---|
| WP0 | `tests/test_hook_enablement_consistency.py`, `tests/test_orchestrator_guard_hook.py`, `tests/test_hook_drift.py`, `tests/test_dod_push_check_hook.py` | `python3 scripts/sync.py --validate` |
| WP1 | `tests/test_version_bookkeeping.py`, `tests/test_config_audit.py`; consistency `provider_registry_completeness`; new null-block unit tests | `python3 scripts/sync.py --validate`, `python scripts/sync.py --check`, scenario for any new `project.yaml` option |
| WP2 | `tests/test_pipelines.py`, `tests/test_routing_tool_definitions.py`, provider-specific `test_*.py`, `provider_registry_completeness`; new `commands`/`checkpointing` assertions | `python -m pytest tests/ -q` across Python 3.9/3.11/3.12 |
| WP3 | `tests/test_admin_server.py`, `tests/browser/` (conftest.py), manual a11y verification | start admin-server, manual PUT round-trip per section |
| WP4 | `tests/scenarios/run.sh` (esp. `asserts/21-auto-commit-off-ignores-config.sh`); `tests/test_sync_check_flag.py` | CI dry-run locally; verify workflow path filters |
| WP5 | `tests/test_config_variable_fallbacks.py`, `tests/test_rule_variables.py`, `tests/test_config_audit.py`; placeholder scan on `--validate` | `python3 scripts/sync.py --validate` |
| WP6 | `tests/test_generated_file_drift.py`, `tests/test_generated_file_drift_e2e.py` | `python scripts/sync.py --check` after a simulated drift |
| WP7 | New assertion on `--audit-config` side effects; existing CLI tests | `python scripts/sync.py --audit-config` in a scratch checkout |

**Known test gaps to note (not necessarily fix in this batch):**

- No ratchet test enforcing `extends`+`patches` for 2-platform composition; 8 full-replacement files remain (6× `agent-meta-*-expert.md`, `sharkord-release.md`, `sharkord-docker.md`) — `tests/test_composition_engine.py`, `tests/test_platform_hacs_preset.py`, `tests/test_frontmatter_*.py` cover adjacent behavior only.
- No `.pre-commit-config.yaml` / `pyproject.toml`; Python 3.9 AST check exists (`scripts/lib/consistency/python_compat.py`) and is wired into `--validate`, enforced by `tests/test_python_compat_consistency.py`.
- `tests/scenarios/run.sh` is not in CI — addressed by WP4.

---

## 5. Entschieden (Decisions confirmed 2026-09-11)

1. **#712 — P1, closed; residual fixed.** P0 label removed, P1 set. Core already fixed on main via #723 / commit `9381bcaa` (`check_hook_enablement_consistency`); sync-ordering bug not reproducible. XS residual IMPLEMENTED in WP0: repo-root `.gitattributes` entry `hooks/1-generic/orchestrator-guard*.sh text eol=lf` (forces LF; generator-first check negative → created directly). Issue CLOSED as completed with a resolution comment.
2. **#730 — P1; issue body corrected.** P1 label set. Body now records that `permission-mode-overrides` IS already allowed (`scripts/admin-server.py:4624`); the genuinely missing writable sections are `hooks`, `debug-mode`, `tier-overrides`, `mcp-role-overrides`, `backup`.
3. **#739 — P2.** P1 label removed, P2 set. Comment added: core fixed by #742 / commit `56da703e`; remaining scope is CI hardening only (WP4).
4. **#742 — INVALID.** It is an already-merged PR (not an issue). No issue to close; a PR comment was posted. Excluded from the issue burn-down.
5. **#736 — issue body corrected.** `agents/1-generic/feature.md` does not exist at all — the drift is larger than originally reported.
6. **Pre-commit gate — CI-only.** No `.pre-commit-config.yaml` will be created; WP4 adds `python3 scripts/sync.py --check` to `.github/workflows/validate.yml`. (Resolves former §5 item 6.)

### Verbleibende Risiken (offen)

1. **Audit-doc/main version gap.** The source audit targets 0.101.0-beta.4 (2026-09-03); main is 1.1.0 (2026-09-11). Much was already remediated. *Risk:* re-triaging already-fixed findings wastes effort and inflates the open count. Recommendation: treat all `already-fixed-on-main (core)` rows as residual-only and re-verify any future findings against HEAD before sizing.
2. **SE-cascade disable decision (#652).** Whether the SE-cascade remains enabled is unresolved and affects template/validation expectations for the `validator`/`requirements` roles used in acceptance traces. *Decision needed:* confirm #652 outcome before WP1/WP5 acceptance tests assume SE-mode behavior.
3. **Collision risk in `config.py`.** Six issues (#731, #732, #733, #736, #737, #741) touch the same module. Mitigation: strict WP1 → WP5 sequencing; WP1 is the only package allowed to add null-safety, so later packages rebase cleanly.
4. **Provider coverage ceiling for WP2.** Full `checkpointing`/chat-push behavior for Gemini/Opencode depends on 674 phase 3.1 (Antigravity/provider hooks). WP2 can deliver capability-driven behavior, but "all providers functional" may remain gated by #674 — scope WP2 accordingly.
5. **Scenario suite in CI cost.** WP4 assumes `tests/scenarios/run.sh` is cheap enough for CI. If it is not, WP4 must file a follow-up rather than silently drop coverage (explicit acceptance criterion in WP4).
