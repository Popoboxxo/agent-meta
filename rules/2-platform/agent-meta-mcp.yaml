platform: agent-meta

# MCP server bundle for the agent-meta meta-repository.
# References entries from config/plugin-catalog.yaml.
# sync.py activates these servers for all projects with platforms: [agent-meta].

mcp-servers:
  - viz-logger
