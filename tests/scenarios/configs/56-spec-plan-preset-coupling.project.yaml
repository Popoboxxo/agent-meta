agent-meta-version: "1.1.0"
ai-providers:
- Claude
# No explicit spec-plan-workflow block at all: the effective switch comes from
# the DoD preset coupling (concept-driven -> spec-plan-required: true).
dod-preset: concept-driven
platforms: []
rules-preset: lazy
knowledge-engine:
  enabled: false
project:
  name: scenario-spec-plan-preset
  prefix: s56
  short: scenario-spec-plan-preset
variables:
  PROJECT_NAME: scenario-spec-plan-preset
  PROJECT_DESCRIPTION: DoD preset coupling without an explicit workflow block.
  PROJECT_GOAL: Verify concept-driven activates the workflow by preset while rapid-prototyping stays a no-op.
  GIT_PLATFORM: GitHub
  GIT_REMOTE_URL: https://github.com/example/scenario-spec-plan-preset
  GIT_MAIN_BRANCH: main
